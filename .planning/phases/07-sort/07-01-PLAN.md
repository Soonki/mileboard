---
phase: 07-sort
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/sort.ts
  - src/utils/sortUtils.ts
  - src/utils/sortUtils.test.ts
  - src/services/sortStorage.ts
  - src/services/sortStorage.test.ts
  - src/stores/sortStore.ts
  - src/stores/sortStore.test.ts
autonomous: true
requirements:
  - SORT-01
  - SORT-02
  - SORT-04
must_haves:
  truths:
    - "applySortToIssuesがassignee.nameのlocaleCompare('ja')で担当者順ソートする"
    - "applySortToIssuesがdueDate文字列比較で期限日順ソートする"
    - "field='none'のときkeyId昇順で返す"
    - "assignee=nullおよびdueDate=nullのカードがdirectionに関係なく常に末尾に配置される"
    - "sortStoreのsetField/toggleDirectionがplugin-storeに即時保存する"
    - "sortStoreのloadFromStorageがplugin-storeからSortConfigを復元する"
    - "不正なplugin-storeデータ時にfield='none', direction='asc'にフォールバックする"
  artifacts:
    - path: "src/types/sort.ts"
      provides: "SortField, SortDirection, SortConfig型定義"
      exports: ["SortField", "SortDirection", "SortConfig"]
    - path: "src/utils/sortUtils.ts"
      provides: "applySortToIssues純粋関数"
      exports: ["applySortToIssues"]
    - path: "src/services/sortStorage.ts"
      provides: "loadSortConfig, saveSortConfig永続化関数"
      exports: ["loadSortConfig", "saveSortConfig"]
    - path: "src/stores/sortStore.ts"
      provides: "useSortStore Zustandストア"
      exports: ["useSortStore"]
  key_links:
    - from: "src/stores/sortStore.ts"
      to: "src/services/sortStorage.ts"
      via: "saveSortConfig/loadSortConfig呼び出し"
      pattern: "saveSortConfig|loadSortConfig"
    - from: "src/stores/sortStore.ts"
      to: "src/types/sort.ts"
      via: "SortField, SortDirection型import"
      pattern: "import.*from.*types/sort"
    - from: "src/utils/sortUtils.ts"
      to: "src/types/sort.ts"
      via: "SortField, SortDirection型import"
      pattern: "import.*from.*types/sort"
    - from: "src/utils/sortUtils.ts"
      to: "src/types/backlog.ts"
      via: "BacklogIssue型import"
      pattern: "import.*BacklogIssue.*from.*types/backlog"
---

<objective>
ソート機能の基盤を構築する: 型定義、ソートロジック純粋関数、plugin-store永続化、Zustand sortStore。

Purpose: Plan 02でUI統合する前に、ソートの状態管理・ロジック・永続化の全バックエンド層をテスト付きで確立する。Phase 6のfilterStore/filterUtils/settingsStorageパターンを踏襲。
Output: sort.ts型、sortUtils.ts純粋関数、sortStorage.ts永続化、sortStore.tsストア + 全テスト
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-sort/07-CONTEXT.md
@.planning/phases/07-sort/07-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/types/backlog.ts:
```typescript
export interface BacklogIssue {
  id: number;
  projectId: number;
  issueKey: string;
  keyId: number;
  summary: string;
  description: string | null;
  status: BacklogStatus;
  priority: BacklogPriority | null;
  assignee: BacklogUser | null;
  milestone: BacklogMilestone[];
  category: BacklogCategory[];
  startDate: string | null;
  dueDate: string | null;
  created: string;
  updated: string;
}

export interface BacklogUser {
  id: number;
  userId: string;
  name: string;
  roleType: number;
  mailAddress: string;
}
```

From src/types/filter.ts (pattern reference):
```typescript
export interface FilterState {
  statusIds: Set<number>;
  assigneeIds: Set<number | null>;
  categoryIds: Set<number>;
}
```

From src/stores/filterStore.ts (Zustand store pattern):
```typescript
export const useFilterStore = create<FilterStoreState>()((set, get) => ({
  // state fields...
  toggleStatus: (id) => set((state) => { /* new Set immutable */ }),
  clearAll: () => set({ /* reset fields */ }),
  hasActiveFilters: () => { const state = get(); /* check sizes */ },
}));
```

From src/services/settingsStorage.ts (plugin-store pattern):
```typescript
import { load } from '@tauri-apps/plugin-store';
const STORE_FILE = 'settings.json';
const SETTINGS_KEY = 'connection';

export async function loadSettings(): Promise<ConnectionSettings | null> {
  const store = await load(STORE_FILE, { autoSave: false });
  const settings = await store.get<ConnectionSettings>(SETTINGS_KEY);
  return settings ?? null;
}

export async function saveSettings(settings: ConnectionSettings): Promise<void> {
  const store = await load(STORE_FILE, { autoSave: false });
  await store.set(SETTINGS_KEY, settings);
  await store.save();  // CRITICAL: must call save()
}
```

From src/utils/filterUtils.ts (pure function pattern):
```typescript
export function applyFilters(
  issues: ReadonlyArray<BacklogIssue>,
  filters: FilterState,
): BacklogIssue[] {
  return issues.filter((issue) => { /* ... */ });
}
```

From tests/setup.ts (plugin-store mock, already global):
```typescript
vi.mock('@tauri-apps/plugin-store', () => ({
  load: vi.fn().mockResolvedValue({
    get: vi.fn().mockResolvedValue(undefined),
    set: vi.fn().mockResolvedValue(undefined),
    save: vi.fn().mockResolvedValue(undefined),
  }),
}));
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SortField/SortDirection/SortConfig型定義 + applySortToIssues純粋関数</name>
  <files>src/types/sort.ts, src/utils/sortUtils.ts, src/utils/sortUtils.test.ts</files>
  <read_first>
    - src/types/backlog.ts (BacklogIssue, BacklogUser型 -- assignee/dueDate/keyIdフィールド確認)
    - src/types/filter.ts (型定義パターン参照)
    - src/utils/filterUtils.ts (applyFilters純粋関数パターン、ReadonlyArray引数、localeCompare('ja')使用)
    - src/utils/filterUtils.test.ts (テストヘルパーcreateIssue関数パターン)
  </read_first>
  <behavior>
    - Test: applySortToIssues(issues, 'none', 'asc') returns issues sorted by keyId ascending
    - Test: applySortToIssues(issues, 'assignee', 'asc') returns issues sorted by assignee.name localeCompare('ja') ascending
    - Test: applySortToIssues(issues, 'assignee', 'desc') returns issues sorted by assignee.name descending
    - Test: applySortToIssues(issues, 'dueDate', 'asc') returns issues sorted by dueDate string ascending (early to late)
    - Test: applySortToIssues(issues, 'dueDate', 'desc') returns issues sorted by dueDate string descending (late to early)
    - Test: assignee=null issues always appear at END regardless of direction='asc' or 'desc'
    - Test: dueDate=null issues always appear at END regardless of direction='asc' or 'desc'
    - Test: among null-value issues, secondary sort is keyId ascending (stable)
    - Test: does NOT mutate the input array (immutability check -- original array order unchanged after call)
    - Test: empty array returns empty array
  </behavior>
  <action>
    **1. src/types/sort.ts を作成:**
    ```typescript
    export type SortField = 'none' | 'assignee' | 'dueDate';
    export type SortDirection = 'asc' | 'desc';

    export interface SortConfig {
      field: SortField;
      direction: SortDirection;
    }
    ```

    **2. src/utils/sortUtils.ts を作成:**
    - `applySortToIssues(issues: ReadonlyArray<BacklogIssue>, field: SortField, direction: SortDirection): BacklogIssue[]` を実装
    - `ReadonlyArray<BacklogIssue>` を引数に取ることでTypeScriptレベルでのmutation防止
    - field='none' の場合: `[...issues].sort((a, b) => a.keyId - b.keyId)` でkeyId昇順（per D-04）
    - field='assignee' の場合:
      - null/non-null にpartition
      - non-null部分: `a.assignee!.name.localeCompare(b.assignee!.name, 'ja')` で比較。direction='desc'なら `cmp * -1`
      - null部分: `a.keyId - b.keyId` で安定ソート
      - `[...withValue, ...withNull]` で結合（D-07: null常に末尾）
    - field='dueDate' の場合:
      - null/non-null にpartition
      - non-null部分: `a.dueDate!.localeCompare(b.dueDate!)` で比較（ISO 8601文字列）。direction='desc'なら `cmp * -1`
      - null部分: `a.keyId - b.keyId` で安定ソート
      - `[...withValue, ...withNull]` で結合（D-07: null常に末尾）
    - 注意: `Array.sort()` はin-place mutation。必ず `[...issues]` または partition後の新配列で操作

    **3. src/utils/sortUtils.test.ts を作成:**
    - filterUtils.test.ts の `createIssue` ヘルパーパターンを踏襲
    - テストデータ: assignee有/無、dueDate有/無、keyId異なる値の組み合わせ
    - 上記 `<behavior>` の全ケースをカバー
  </action>
  <verify>
    <automated>npx vitest run src/utils/sortUtils.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - src/types/sort.ts が存在し `export type SortField = 'none' | 'assignee' | 'dueDate'` を含む
    - src/types/sort.ts が `export type SortDirection = 'asc' | 'desc'` を含む
    - src/types/sort.ts が `export interface SortConfig` を含む
    - src/utils/sortUtils.ts が `export function applySortToIssues(` を含む
    - src/utils/sortUtils.ts が `ReadonlyArray<BacklogIssue>` を引数型に含む
    - src/utils/sortUtils.ts が `localeCompare` を含む（担当者名ソート用）
    - src/utils/sortUtils.ts が `'ja'` を含む（日本語locale）
    - src/utils/sortUtils.test.ts が10件以上のテストケースを含む
    - `npx vitest run src/utils/sortUtils.test.ts` が exit code 0
  </acceptance_criteria>
  <done>SortField/SortDirection/SortConfig型がエクスポートされ、applySortToIssuesが担当者順・期限日順・ソートなしの全パターンでnull末尾処理含め正しく動作し、immutabilityが保証され、全テスト通過</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: sortStorage永続化 + useSortStore Zustandストア</name>
  <files>src/services/sortStorage.ts, src/services/sortStorage.test.ts, src/stores/sortStore.ts, src/stores/sortStore.test.ts</files>
  <read_first>
    - src/services/settingsStorage.ts (plugin-store永続化パターン: load/set/save)
    - src/stores/filterStore.ts (Zustand storeパターン: create, set, get)
    - src/stores/settingsStore.ts (loadFromStorage非同期パターン)
    - src/types/sort.ts (Task 1で作成した型)
    - tests/setup.ts (グローバルmock確認: plugin-storeはmock済み)
    - src/stores/filterStore.test.ts (storeテストパターン参照)
  </read_first>
  <behavior>
    sortStorage:
    - Test: loadSortConfig returns SortConfig when plugin-store has valid data
    - Test: loadSortConfig returns null when plugin-store has no data
    - Test: loadSortConfig returns null when plugin-store data has invalid field value
    - Test: saveSortConfig calls store.set with key 'sort' and config, then calls store.save()

    sortStore:
    - Test: initial state is field='none', direction='asc'
    - Test: setField('assignee') updates field to 'assignee' and calls saveSortConfig
    - Test: setField('none') resets field to 'none'
    - Test: toggleDirection switches 'asc' to 'desc' and calls saveSortConfig
    - Test: toggleDirection switches 'desc' back to 'asc'
    - Test: loadFromStorage restores field and direction from plugin-store
    - Test: loadFromStorage with null config keeps default state
    - Test: loadFromStorage with invalid data keeps default state (fallback)
  </behavior>
  <action>
    **1. src/services/sortStorage.ts を作成:**
    - settingsStorage.tsと同じパターンを踏襲
    - `const STORE_FILE = 'settings.json'` （既存ファイルに別キーを追加）
    - `const SORT_KEY = 'sort'`
    - `loadSortConfig(): Promise<SortConfig | null>`:
      - `const store = await load(STORE_FILE, { autoSave: false })`
      - `const config = await store.get<SortConfig>(SORT_KEY)`
      - バリデーション: `config` が存在し、`config.field` が `'none' | 'assignee' | 'dueDate'` のいずれか、`config.direction` が `'asc' | 'desc'` のいずれかであることを確認
      - 不正値の場合は `null` を返す（T-07-01対応: plugin-storeデータの型バリデーション）
      - 正常な場合は `config` を返す
    - `saveSortConfig(config: SortConfig): Promise<void>`:
      - `const store = await load(STORE_FILE, { autoSave: false })`
      - `await store.set(SORT_KEY, config)`
      - `await store.save()` -- CRITICAL: save()を必ず呼ぶ

    **2. src/services/sortStorage.test.ts を作成:**
    - tests/setup.tsのグローバルmockがplugin-storeをmock済みなので、vi.mocked(load)でstore.get/set/saveの挙動を制御
    - load/save/バリデーションの全ケースをテスト

    **3. src/stores/sortStore.ts を作成:**
    ```typescript
    import { create } from 'zustand';
    import type { SortField, SortDirection } from '../types/sort';
    import { loadSortConfig, saveSortConfig } from '../services/sortStorage';

    interface SortStoreState {
      field: SortField;
      direction: SortDirection;
      setField: (field: SortField) => void;
      toggleDirection: () => void;
      loadFromStorage: () => Promise<void>;
    }

    export const useSortStore = create<SortStoreState>()((set, get) => ({
      field: 'none',
      direction: 'asc',

      setField: (field) => {
        set({ field });
        saveSortConfig({ field, direction: get().direction });
      },

      toggleDirection: () => {
        const next = get().direction === 'asc' ? 'desc' : 'asc';
        set({ direction: next });
        saveSortConfig({ field: get().field, direction: next });
      },

      loadFromStorage: async () => {
        const config = await loadSortConfig();
        if (config) {
          set({ field: config.field, direction: config.direction });
        }
      },
    }));
    ```
    - setFieldとtoggleDirectionはfire-and-forget でsaveSortConfigを呼ぶ（awaitしない）
    - loadFromStorageでconfigがnull（データなし or バリデーション失敗）ならデフォルト値のまま

    **4. src/stores/sortStore.test.ts を作成:**
    - sortStorageをvi.mockし、loadSortConfig/saveSortConfigの呼び出しを検証
    - 各テスト前にstoreをリセット（useSortStore.setState({ field: 'none', direction: 'asc' })）
    - setField/toggleDirection/loadFromStorageの全パターンをテスト
  </action>
  <verify>
    <automated>npx vitest run src/services/sortStorage.test.ts src/stores/sortStore.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - src/services/sortStorage.ts が存在し `export async function loadSortConfig` を含む
    - src/services/sortStorage.ts が `export async function saveSortConfig` を含む
    - src/services/sortStorage.ts が `store.save()` 呼び出しを含む
    - src/services/sortStorage.ts が `SORT_KEY` = `'sort'` を含む
    - src/services/sortStorage.ts が `STORE_FILE` = `'settings.json'` を含む
    - src/services/sortStorage.ts がfield値のバリデーション（'none','assignee','dueDate'のいずれか）を含む
    - src/stores/sortStore.ts が存在し `export const useSortStore = create` を含む
    - src/stores/sortStore.ts が `field: 'none'` と `direction: 'asc'` のデフォルト値を含む
    - src/stores/sortStore.ts が `setField`, `toggleDirection`, `loadFromStorage` メソッドを含む
    - src/stores/sortStore.ts が `saveSortConfig` を呼び出す
    - `npx vitest run src/services/sortStorage.test.ts src/stores/sortStore.test.ts` が exit code 0
  </acceptance_criteria>
  <done>sortStorageがplugin-storeのsettings.jsonファイルにsortキーでSortConfigを永続化し、不正値バリデーション付き。useSortStoreがfield/directionの状態管理+即時永続化+起動時復元を提供し、全テスト通過</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin-store -> sortStore | ディスクから読み込んだソート設定が不正値の可能性 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-01 | Tampering | sortStorage.ts loadSortConfig | mitigate | loadSortConfig内でconfig.fieldが'none'/'assignee'/'dueDate'のいずれか、config.directionが'asc'/'desc'のいずれかであることをバリデーション。不正値はnullを返しデフォルト値にフォールバック |
| T-07-02 | Information Disclosure | sortStorage.ts | accept | ソート設定はユーザーのUI嗜好のみ（PII/機密情報なし）。plugin-storeはローカルファイルでリモート送信なし |
</threat_model>

<verification>
- `npx vitest run src/utils/sortUtils.test.ts src/services/sortStorage.test.ts src/stores/sortStore.test.ts` が全パス
- src/types/sort.ts に SortField, SortDirection, SortConfig がexport
- applySortToIssuesがassignee/dueDate/noneの全パターンでnull末尾処理含め正しく動作
- sortStorageがsettings.jsonの'sort'キーで永続化し、不正値をバリデーション
- sortStoreのsetField/toggleDirectionが即時永続化
</verification>

<success_criteria>
- SortField/SortDirection/SortConfig型定義が完成
- applySortToIssuesが全ソートパターン（担当者/期限日/なし x 昇順/降順 x null値）で正しく動作
- sortStorageがplugin-storeで永続化+バリデーション
- sortStoreが状態管理+永続化+起動時復元を提供
- 全テストパス、immutability保証
</success_criteria>

<output>
After completion, create `.planning/phases/07-sort/07-01-SUMMARY.md`
</output>
