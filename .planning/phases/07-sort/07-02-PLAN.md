---
phase: 07-sort
plan: 02
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - src/components/SortDropdown/SortDropdown.tsx
  - src/components/SortDropdown/SortDropdown.module.css
  - src/components/SortDropdown/SortDropdown.test.tsx
  - src/components/FilterBar/FilterBar.tsx
  - src/components/FilterBar/FilterBar.module.css
  - src/components/FilterBar/FilterBar.test.tsx
  - src/components/Board/Board.tsx
  - src/components/Board/Board.test.tsx
  - src/App.tsx
autonomous: false
requirements:
  - SORT-01
  - SORT-02
  - SORT-03
  - SORT-04
must_haves:
  truths:
    - "SortDropdownが3つの選択肢（ソートなし/担当者順/期限日順）を単一選択で表示する"
    - "ソートドロップダウンの横に上下トグルボタンが表示され、クリックで昇順と降順が切り替わる"
    - "FilterBarの右側にセパレーター+ソートコントロールが配置される"
    - "Board.tsxのuseMemoがフィルタからソートの順でカード配列を処理する"
    - "ソート設定がアプリ起動時にplugin-storeから復元される"
    - "全レーンのカードに一括でソートが適用される"
    - "DnDハンドラはunfilteredデータのみを参照し続ける"
  artifacts:
    - path: "src/components/SortDropdown/SortDropdown.tsx"
      provides: "ソート基準選択ドロップダウン（単一選択）"
      exports: ["SortDropdown"]
    - path: "src/components/SortDropdown/SortDropdown.module.css"
      provides: "SortDropdownスタイル"
    - path: "src/components/FilterBar/FilterBar.tsx"
      provides: "セパレーター+SortDropdown+方向トグル統合"
    - path: "src/components/Board/Board.tsx"
      provides: "useMemoにソート処理追加"
  key_links:
    - from: "src/components/SortDropdown/SortDropdown.tsx"
      to: "src/stores/sortStore.ts"
      via: "useSortStoreセレクタ"
      pattern: "useSortStore"
    - from: "src/components/FilterBar/FilterBar.tsx"
      to: "src/components/SortDropdown/SortDropdown.tsx"
      via: "SortDropdownコンポーネントimport"
      pattern: "import.*SortDropdown"
    - from: "src/components/Board/Board.tsx"
      to: "src/utils/sortUtils.ts"
      via: "applySortToIssues import"
      pattern: "import.*applySortToIssues"
    - from: "src/components/Board/Board.tsx"
      to: "src/stores/sortStore.ts"
      via: "useSortStoreセレクタ"
      pattern: "useSortStore"
    - from: "src/App.tsx"
      to: "src/stores/sortStore.ts"
      via: "loadFromStorage呼び出し"
      pattern: "useSortStore"
---

<objective>
SortDropdown UIコンポーネント + FilterBarへのソートUI統合 + Board.tsxのuseMemoパイプライン拡張 + App.tsxでのsortStore初期化を完了し、ソート機能をエンドツーエンドで動作させる。

Purpose: Plan 01で構築した型/ロジック/ストア基盤をUIに接続し、ユーザーがソートドロップダウンと方向トグルで全レーンのカード並び順を制御できるようにする。
Output: SortDropdownコンポーネント、FilterBarソートUI統合、Board.tsxソートパイプライン、App.tsx起動時復元 + 全テスト
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-sort/07-CONTEXT.md
@.planning/phases/07-sort/07-RESEARCH.md
@.planning/phases/07-sort/07-UI-SPEC.md
@.planning/phases/07-sort/07-01-SUMMARY.md

<interfaces>
<!-- Key types and contracts from Plan 01 output and existing codebase. -->

From src/types/sort.ts (Plan 01 output):
```typescript
export type SortField = 'none' | 'assignee' | 'dueDate';
export type SortDirection = 'asc' | 'desc';
export interface SortConfig {
  field: SortField;
  direction: SortDirection;
}
```

From src/stores/sortStore.ts (Plan 01 output):
```typescript
interface SortStoreState {
  field: SortField;
  direction: SortDirection;
  setField: (field: SortField) => void;
  toggleDirection: () => void;
  loadFromStorage: () => Promise<void>;
}
export const useSortStore = create<SortStoreState>()((set, get) => ({
  field: 'none',
  direction: 'asc',
  setField: (field) => { set({ field }); saveSortConfig({ field, direction: get().direction }); },
  toggleDirection: () => { const next = get().direction === 'asc' ? 'desc' : 'asc'; set({ direction: next }); saveSortConfig({ field: get().field, direction: next }); },
  loadFromStorage: async () => { const config = await loadSortConfig(); if (config) { set({ field: config.field, direction: config.direction }); } },
}));
```

From src/utils/sortUtils.ts (Plan 01 output):
```typescript
export function applySortToIssues(
  issues: ReadonlyArray<BacklogIssue>,
  field: SortField,
  direction: SortDirection,
): BacklogIssue[];
```

From src/components/FilterBar/FilterBar.tsx (current):
```typescript
export function FilterBar() {
  // ... uses useBoardStore, useFilterStore
  return (
    <div className={styles.filterBar} role="toolbar" aria-label="フィルタ">
      <div className={styles.dropdowns}>
        <FilterDropdown label="ステータス" ... />
        <FilterDropdown label="担当者" ... />
        <FilterDropdown label="カテゴリ" ... />
      </div>
      <div className={styles.chips}> ... </div>
    </div>
  );
}
```

From src/components/FilterDropdown/FilterDropdown.tsx (UI pattern reference):
```typescript
interface FilterDropdownProps {
  label: string;
  options: FilterOption[];
  selectedIds: Set<number | null>;
  onToggle: (id: number | null) => void;
}
// Uses: useState(isOpen), useRef(containerRef), click-outside handler, keyboard navigation
// Trigger: button with aria-haspopup="listbox", aria-expanded
// Panel: div with role="listbox", aria-multiselectable="true"
// Each option: div with role="option", aria-selected
```

From src/components/Board/Board.tsx (current useMemo):
```typescript
const filteredView = useMemo(() => {
  if (!data) return null;
  const filters: FilterState = { statusIds, assigneeIds, categoryIds };
  const hasFilters = statusIds.size > 0 || assigneeIds.size > 0 || categoryIds.size > 0;
  const unassignedFiltered = hasFilters ? applyFilters(data.unassignedIssues, filters) : data.unassignedIssues;
  return {
    milestones: data.milestones.map((mwi) => {
      const filtered = hasFilters ? applyFilters(mwi.issues, filters) : mwi.issues;
      return { milestone: mwi.milestone, filteredIssues: filtered, hiddenCount: ... };
    }),
    unassigned: { filteredIssues: unassignedFiltered, hiddenCount: ... },
  };
}, [data, statusIds, assigneeIds, categoryIds]);
```

From src/App.tsx (current):
```typescript
function App() {
  const loadFromStorage = useSettingsStore((s) => s.loadFromStorage);
  useEffect(() => { loadFromStorage(); }, [loadFromStorage]);
  // ...
  return (
    <div className={styles.appLayout}>
      <BoardHeader onSettingsOpen={...} />
      <FilterBar />
      <Board />
    </div>
  );
}
```

From src/components/FilterBar/FilterBar.module.css (current):
```css
.filterBar { display: flex; flex-wrap: wrap; align-items: center; gap: var(--space-sm); padding: var(--space-xs) var(--space-md); min-height: 44px; background: var(--color-surface); border-bottom: 1px solid var(--color-border); position: sticky; top: 56px; z-index: 9; }
.dropdowns { display: flex; gap: var(--space-sm); align-items: center; }
.chips { display: flex; flex-wrap: wrap; gap: var(--space-xs); align-items: center; margin-left: var(--space-sm); }
```

From src/components/FilterDropdown/FilterDropdown.module.css (style reference):
```css
.trigger { min-width: 100px; height: 32px; border: 1px solid var(--color-border); border-radius: var(--radius-input); padding: var(--space-xs) var(--space-sm); font-size: var(--font-size-body); cursor: pointer; background: var(--color-surface); display: flex; align-items: center; gap: var(--space-xs); transition: border-color var(--transition-fast); }
.trigger:hover { border-color: var(--color-accent); }
.panel { position: absolute; top: 100%; left: 0; margin-top: var(--space-xs); min-width: 100%; max-height: 240px; overflow-y: auto; background: var(--color-surface); border: 1px solid var(--color-border); border-radius: var(--radius-input); box-shadow: var(--shadow-dropdown); z-index: 20; }
.option { padding: var(--space-xs) var(--space-sm); display: flex; align-items: center; gap: var(--space-sm); cursor: pointer; }
.option:hover, .optionFocused { background: var(--color-dropdown-hover); }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: SortDropdownコンポーネント + FilterBarソートUI統合 + FilterBar.module.css拡張</name>
  <files>src/components/SortDropdown/SortDropdown.tsx, src/components/SortDropdown/SortDropdown.module.css, src/components/SortDropdown/SortDropdown.test.tsx, src/components/FilterBar/FilterBar.tsx, src/components/FilterBar/FilterBar.module.css, src/components/FilterBar/FilterBar.test.tsx</files>
  <read_first>
    - src/components/FilterDropdown/FilterDropdown.tsx (UIパターン参照: trigger/panel/option構造、click-outside、keyboard navigation)
    - src/components/FilterDropdown/FilterDropdown.module.css (スタイルパターン参照: trigger/panel/option CSS)
    - src/components/FilterBar/FilterBar.tsx (現在のFilterBar構造 -- ソートUI追加先)
    - src/components/FilterBar/FilterBar.module.css (現在のFilterBar CSS -- セパレーター/sortControls追加先)
    - src/components/FilterBar/FilterBar.test.tsx (既存テスト構造 -- ソートUIテスト追加)
    - src/stores/sortStore.ts (Plan 01で作成 -- useSortStoreのfield/direction/setField/toggleDirection)
    - src/types/sort.ts (Plan 01で作成 -- SortField型)
    - .planning/phases/07-sort/07-UI-SPEC.md (UIデザイン契約: SortDropdownレイアウト、方向トグル、セパレーター仕様)
  </read_first>
  <behavior>
    SortDropdown:
    - Test: renders trigger button with text "ソートなし" when field='none'
    - Test: renders trigger button with text "担当者順" when field='assignee'
    - Test: renders trigger button with text "期限日順" when field='dueDate'
    - Test: trigger has aria-haspopup="listbox" and aria-expanded="false"
    - Test: clicking trigger opens panel with 3 options
    - Test: panel has role="listbox" without aria-multiselectable
    - Test: each option has role="option"
    - Test: clicking option calls setField and closes panel
    - Test: selected option has aria-selected="true" and highlighted background class
    - Test: Escape key closes panel and returns focus to trigger
    - Test: ArrowDown/Up navigates options, Enter selects
    - Test: clicking outside closes panel

    FilterBar integration:
    - Test: renders separator between filter dropdowns and sort controls
    - Test: renders SortDropdown component
    - Test: renders direction toggle button with aria-label
    - Test: direction toggle shows U+2191 for asc, U+2193 for desc
    - Test: clicking direction toggle calls toggleDirection
    - Test: toolbar aria-label is "フィルタとソート"
  </behavior>
  <action>
    **1. src/components/SortDropdown/SortDropdown.tsx を作成:**
    - FilterDropdown.tsxのUIパターンを参考にするが、**新規コンポーネント**として作成（FilterDropdownは改造しない）
    - props不要: useSortStore((s) => s.field)とuseSortStore((s) => s.setField)を直接使用
    - 選択肢定数:
      ```typescript
      const SORT_OPTIONS: ReadonlyArray<{ value: SortField; label: string }> = [
        { value: 'none', label: 'ソートなし' },
        { value: 'assignee', label: '担当者順' },
        { value: 'dueDate', label: '期限日順' },
      ] as const;
      ```
    - state: `const [isOpen, setIsOpen] = useState(false)` と `const [focusedIndex, setFocusedIndex] = useState(-1)`
    - refs: containerRef, listRef, triggerRef
    - click-outside handler: FilterDropdownと同じuseEffect + document.addEventListener('mousedown') + containerRef.contains
    - パネル開時自動フォーカス: `useEffect(() => { if (isOpen && listRef.current) listRef.current.focus(); }, [isOpen])`
    - trigger button:
      - `aria-haspopup="listbox"`, `aria-expanded={isOpen}`, `aria-label="ソート基準"`
      - テキスト: 選択中のオプションのlabel（'none'なら'ソートなし'）
      - 矢印: `'\u25BE'` (small down triangle)
      - className: `styles.trigger` + (field !== 'none' ? `styles.active` : `styles.inactive`)
      - min-width: 110px
    - panel:
      - `role="listbox"` (**aria-multiselectableなし** -- 単一選択、Pitfall 6回避)
      - `aria-activedescendant={focusedIndex >= 0 ? \`sort-option-${SORT_OPTIONS[focusedIndex].value}\` : undefined}`
    - option:
      - `role="option"`, `aria-selected={option.value === field}`
      - `id={\`sort-option-${option.value}\`}`
      - クリック時: `setField(option.value)` + `close()` （選択でパネル閉じ -- FilterDropdownとの違い）
      - 選択中optionは `styles.optionSelected` クラスでbackground: var(--color-dropdown-hover)
    - keyboard:
      - Escape: close() + triggerRef.current?.focus()
      - ArrowDown: `setFocusedIndex((prev) => (prev + 1) % SORT_OPTIONS.length)`
      - ArrowUp: `setFocusedIndex((prev) => prev <= 0 ? SORT_OPTIONS.length - 1 : prev - 1)`
      - Enter/Space: focusedIndex >= 0 なら `setField(SORT_OPTIONS[focusedIndex].value)` + close()

    **2. src/components/SortDropdown/SortDropdown.module.css を作成:**
    - FilterDropdown.module.cssをベースにスタイリングを統一
    - `.container { position: relative; }`
    - `.trigger`: FilterDropdown.triggerと同じスタイル + `min-width: 110px;` (UI-SPEC: 110px)
    - `.trigger:hover { border-color: var(--color-accent); }`
    - `.active { color: var(--color-text-primary); }`
    - `.inactive { color: var(--color-text-secondary); }`
    - `.arrow { margin-left: auto; font-size: var(--font-size-sm); }`
    - `.panel`: FilterDropdown.panelと同じスタイル（absolute, z-index:20, shadow-dropdown）
      - `opacity: 1; transform: translateY(0); transition: opacity var(--transition-fast), transform var(--transition-fast);`
    - `.option`: FilterDropdown.optionと同じ基本スタイル。チェックボックスなし
      - `padding: var(--space-xs) var(--space-sm);`
      - `cursor: pointer;`
    - `.option:hover, .optionFocused { background: var(--color-dropdown-hover); }`
    - `.optionSelected { background: var(--color-dropdown-hover); }` (選択中のオプション常時ハイライト)
    - `.optionLabel { font-size: var(--font-size-label); color: var(--color-text-primary); }`

    **3. src/components/FilterBar/FilterBar.tsx を修正:**
    - import追加: `import { SortDropdown } from '../SortDropdown/SortDropdown';`
    - import追加: `import { useSortStore } from '../../stores/sortStore';`
    - sort storeセレクタ追加:
      ```typescript
      const sortField = useSortStore((s) => s.field);
      const sortDirection = useSortStore((s) => s.direction);
      const toggleDirection = useSortStore((s) => s.toggleDirection);
      ```
    - `role="toolbar"` の `aria-label` を `"フィルタとソート"` に変更
    - `.dropdowns` div内の3つのFilterDropdown の後に:
      ```tsx
      <div className={styles.separator} role="separator" aria-orientation="vertical" />
      <div className={styles.sortControls}>
        <SortDropdown />
        <button
          type="button"
          className={`${styles.directionToggle} ${sortField !== 'none' ? styles.directionActive : styles.directionInactive}`}
          onClick={toggleDirection}
          aria-label={sortDirection === 'asc' ? '降順に切り替え' : '昇順に切り替え'}
        >
          {sortDirection === 'asc' ? '\u2191' : '\u2193'}
        </button>
      </div>
      ```

    **4. src/components/FilterBar/FilterBar.module.css を修正:**
    - 末尾に追加:
      ```css
      .separator {
        width: 1px;
        height: 20px;
        background: var(--color-border);
        align-self: center;
        margin: 0 var(--space-sm);
        flex-shrink: 0;
      }

      .sortControls {
        display: flex;
        gap: var(--space-sm);
        align-items: center;
      }

      .directionToggle {
        width: 32px;
        height: 32px;
        border: 1px solid var(--color-border);
        border-radius: var(--radius-input);
        background: var(--color-surface);
        font-size: var(--font-size-sm);
        cursor: pointer;
        display: flex;
        align-items: center;
        justify-content: center;
        transition: border-color var(--transition-fast), color var(--transition-fast);
      }

      .directionToggle:hover {
        border-color: var(--color-accent);
      }

      .directionActive {
        color: var(--color-text-primary);
        cursor: pointer;
      }

      .directionInactive {
        color: var(--color-disabled, #9CA3AF);
        cursor: default;
      }
      ```

    **5. src/components/SortDropdown/SortDropdown.test.tsx を作成:**
    - sortStoreをvi.mockし、mockSortStateオブジェクトでfield/direction/setField/toggleDirectionを制御
    - FilterBar.test.tsxと同様のmockパターン:
      ```typescript
      const mockSortState = {
        field: 'none' as SortField,
        direction: 'asc' as SortDirection,
        setField: vi.fn(),
        toggleDirection: vi.fn(),
        loadFromStorage: vi.fn(),
      };
      vi.mock('../../stores/sortStore', () => ({
        useSortStore: (selector: (s: typeof mockSortState) => unknown) => selector(mockSortState),
      }));
      ```
    - 上記の全SortDropdownテストケースを実装

    **6. src/components/FilterBar/FilterBar.test.tsx を修正:**
    - sortStore mockを追加（SortDropdown.test.tsxと同様のmockSortState）
    - 既存テストのaria-label期待値を `'フィルタとソート'` に変更
    - 新規テスト追加: セパレーター描画確認、SortDropdown描画確認、方向トグル描画+クリック確認
  </action>
  <verify>
    <automated>npx vitest run src/components/SortDropdown/SortDropdown.test.tsx src/components/FilterBar/FilterBar.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - src/components/SortDropdown/SortDropdown.tsx が存在し `export function SortDropdown` を含む
    - SortDropdown.tsx が `role="listbox"` を含み `aria-multiselectable` を含まない
    - SortDropdown.tsx が `aria-haspopup="listbox"` を含む
    - SortDropdown.tsx が `aria-label="ソート基準"` を含む
    - SortDropdown.tsx が `'\u25BE'` (down triangle) を含む
    - SortDropdown.tsx が SORT_OPTIONS 配列に 'ソートなし', '担当者順', '期限日順' を含む
    - SortDropdown.module.css が .trigger と min-width: 110px を含む
    - FilterBar.tsx が `import { SortDropdown }` を含む
    - FilterBar.tsx が `import { useSortStore }` を含む
    - FilterBar.tsx が `aria-label="フィルタとソート"` を含む
    - FilterBar.tsx が `role="separator"` を含む
    - FilterBar.tsx が '\u2191' (up arrow) と '\u2193' (down arrow) を含む
    - FilterBar.module.css が .separator と .sortControls と .directionToggle を含む
    - `npx vitest run src/components/SortDropdown/SortDropdown.test.tsx src/components/FilterBar/FilterBar.test.tsx` が exit code 0
  </acceptance_criteria>
  <done>SortDropdownが3選択肢の単一選択ドロップダウンとして動作し、FilterBarの右側にセパレーター+SortDropdown+方向トグルが配置され、キーボードアクセシビリティとARIA属性が完備、全テスト通過</done>
</task>

<task type="auto">
  <name>Task 2: Board.tsx useMemoソートパイプライン拡張 + App.tsx sortStore初期化</name>
  <files>src/components/Board/Board.tsx, src/components/Board/Board.test.tsx, src/App.tsx</files>
  <read_first>
    - src/components/Board/Board.tsx (現在のfilteredView useMemo -- ソート処理追加先)
    - src/components/Board/Board.test.tsx (既存テスト -- ソートテスト追加)
    - src/App.tsx (現在のApp構造 -- sortStore.loadFromStorage呼び出し追加先)
    - src/stores/sortStore.ts (Plan 01で作成 -- useSortStoreのfield/direction/loadFromStorage)
    - src/utils/sortUtils.ts (Plan 01で作成 -- applySortToIssues)
    - src/types/sort.ts (Plan 01で作成 -- SortField, SortDirection型)
  </read_first>
  <action>
    **1. src/components/Board/Board.tsx を修正:**
    - import追加:
      ```typescript
      import { useSortStore } from '../../stores/sortStore';
      import { applySortToIssues } from '../../utils/sortUtils';
      ```
    - Board()関数内、既存のfilterStoreセレクタの後に追加:
      ```typescript
      const sortField = useSortStore((s) => s.field);
      const sortDirection = useSortStore((s) => s.direction);
      ```
    - `filteredView` useMemoを `filteredAndSortedView` にリネームし、ソート処理を追加:
      - 依存配列に `sortField, sortDirection` を追加
      - unassigned: `applyFilters` 後に `applySortToIssues(unassignedFiltered, sortField, sortDirection)` を適用
      - milestones: 各 `mwi` で `applyFilters` 後に `applySortToIssues(filtered, sortField, sortDirection)` を適用
      - filteredIssuesの代わりにソート済み配列をfilteredIssuesとして返す
    - 具体的なuseMemoコード:
      ```typescript
      const filteredAndSortedView = useMemo(() => {
        if (!data) return null;
        const filters: FilterState = { statusIds, assigneeIds, categoryIds };
        const hasFilters =
          statusIds.size > 0 || assigneeIds.size > 0 || categoryIds.size > 0;

        const unassignedFiltered = hasFilters
          ? applyFilters(data.unassignedIssues, filters)
          : data.unassignedIssues;
        const unassignedSorted = applySortToIssues(unassignedFiltered, sortField, sortDirection);

        return {
          milestones: data.milestones.map((mwi) => {
            const filtered = hasFilters
              ? applyFilters(mwi.issues, filters)
              : mwi.issues;
            const sorted = applySortToIssues(filtered, sortField, sortDirection);
            return {
              milestone: mwi.milestone,
              filteredIssues: sorted,
              hiddenCount: hasFilters ? mwi.issues.length - filtered.length : 0,
            };
          }),
          unassigned: {
            filteredIssues: unassignedSorted,
            hiddenCount: hasFilters
              ? data.unassignedIssues.length - unassignedFiltered.length
              : 0,
          },
        };
      }, [data, statusIds, assigneeIds, categoryIds, sortField, sortDirection]);
      ```
    - JSX内の `filteredView` 参照を全て `filteredAndSortedView` に置換
    - **DnDハンドラ（handleDragStart/Over/End）はdata（unfiltered）を引き続き参照 -- D-09/D-10厳守**

    **2. src/App.tsx を修正:**
    - import追加: `import { useSortStore } from './stores/sortStore';`
    - App()関数内、既存のsettingsStore loadFromStorageの後に追加:
      ```typescript
      const loadSortFromStorage = useSortStore((s) => s.loadFromStorage);
      ```
    - 独立したuseEffectとして追加:
      ```typescript
      useEffect(() => {
        loadSortFromStorage();
      }, [loadSortFromStorage]);
      ```

    **3. src/components/Board/Board.test.tsx を修正:**
    - sortStore mockを追加:
      ```typescript
      const mockSortState = {
        field: 'none' as const,
        direction: 'asc' as const,
        setField: vi.fn(),
        toggleDirection: vi.fn(),
        loadFromStorage: vi.fn(),
      };
      vi.mock('../../stores/sortStore', () => ({
        useSortStore: (selector: (s: typeof mockSortState) => unknown) => selector(mockSortState),
      }));
      ```
    - sortUtils mockを追加:
      ```typescript
      vi.mock('../../utils/sortUtils', () => ({
        applySortToIssues: vi.fn((issues: unknown[]) => issues),
      }));
      ```
    - 既存テストが壊れないことを確認（sortFieldが'none'のデフォルトで既存のissue順が維持される）
    - 新規テスト追加:
      - sortField/sortDirectionがuseMemo依存配列に含まれることの間接テスト（sortState変更でapplySortToIssuesが呼ばれること）
  </action>
  <verify>
    <automated>npx vitest run src/components/Board/Board.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - Board.tsx が `import { useSortStore }` を含む
    - Board.tsx が `import { applySortToIssues }` を含む
    - Board.tsx が `useSortStore((s) => s.field)` と `useSortStore((s) => s.direction)` を含む
    - Board.tsx の useMemo 依存配列が `sortField` と `sortDirection` を含む
    - Board.tsx が `applySortToIssues(` を2箇所以上で呼び出す（unassigned + milestones.map内）
    - Board.tsx の DnD ハンドラ (handleDragStart, handleDragOver, handleDragEnd) が `data` のみを参照し `filteredAndSortedView` を参照しない
    - App.tsx が `import { useSortStore }` を含む
    - App.tsx が `loadSortFromStorage` の useEffect 呼び出しを含む
    - `npx vitest run src/components/Board/Board.test.tsx` が exit code 0
  </acceptance_criteria>
  <done>Board.tsxのuseMemoがフィルタからソートの順で全レーンのカードを処理し、App.tsxでsortStore設定がアプリ起動時にplugin-storeから復元され、DnDハンドラはunfilteredデータのみを参照し続け、全テスト通過</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: ソート機能エンドツーエンド動作確認</name>
  <files>N/A</files>
  <action>
    ソート機能の全フローを手動で検証する。自動テストではカバーできないDnD操作、視覚的レイアウト、plugin-store永続化（アプリ再起動）、キーボードアクセシビリティの実動作を確認。
  </action>
  <what-built>ソート機能のエンドツーエンド動作: SortDropdownドロップダウン、方向トグル、全レーンソート適用、plugin-store永続化</what-built>
  <how-to-verify>
    1. `npm run tauri dev` でアプリを起動
    2. ボード表示後、FilterBarの右側にセパレーター（縦線）が見えることを確認
    3. セパレーターの右にソートドロップダウン（「ソートなし」テキスト）と方向トグル（上矢印）が表示されることを確認
    4. ソートドロップダウンをクリック -- 「ソートなし」「担当者順」「期限日順」の3オプションが表示
    5. 「担当者順」を選択 -- 全レーンのカードが担当者名のA-Z順（日本語あいうえお順）に並ぶ。未割り当てカードは末尾
    6. 方向トグル（上矢印）をクリック -- 下矢印に変わり、担当者名がZ-A順になる。未割り当てカードは引き続き末尾
    7. 「期限日順」を選択 -- 全レーンのカードが期限日の早い順に並ぶ。期限なしカードは末尾
    8. 方向トグルをクリック -- 遅い順に変わる。期限なしカードは引き続き末尾
    9. 「ソートなし」を選択 -- カードがデフォルトの課題ID順に戻る
    10. ソート適用中にフィルタを操作 -- フィルタとソートが同時に適用されることを確認
    11. ソート適用中にカードをDnDで別レーンに移動 -- 正常に動作し、移動先でソート位置に配置される
    12. アプリを閉じて再起動 -- ソート設定（例：担当者順/降順）が復元されていることを確認
    13. Tab/Enter/Escapeキーでソートドロップダウンを操作できることを確認
  </how-to-verify>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>全13ステップの手動検証をパスし、ソート機能がエンドツーエンドで正常動作する</done>
  <resume-signal>"approved" と入力、または問題点を記述</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin-store -> App.tsx | アプリ起動時にplugin-storeからソート設定を読み込み、UIに反映 |
| user input -> SortDropdown | ドロップダウン選択値がsetFieldに渡される |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-03 | Tampering | SortDropdown user input | mitigate | SORT_OPTIONS配列の固定値（'none'/'assignee'/'dueDate'）のみsetFieldに渡す。自由入力不可。SortField型がstring literalで制限 |
| T-07-04 | Spoofing | Board.tsx useMemo | accept | ソートはクライアントサイドのみ。boardStore.dataは変更しない（D-10）。APIコール不要 |
</threat_model>

<verification>
- `npx vitest run` で全テストスイートがパス
- SortDropdownが3選択肢の単一選択ドロップダウンとして動作
- FilterBarにセパレーター+SortDropdown+方向トグルが表示
- Board.tsxのuseMemoがフィルタからソートの順で処理
- App.tsxでsortStore.loadFromStorageが起動時に呼ばれる
- DnDハンドラはunfilteredデータのみを参照
</verification>

<success_criteria>
- SortDropdownコンポーネントが単一選択ドロップダウンとして3オプションを表示し、ARIA属性完備
- FilterBarの右側にセパレーター+SortDropdown+方向トグルが配置
- Board.tsxが全レーンのカードにフィルタからソートの順で処理を適用
- App.tsxでアプリ起動時にsortStore設定がplugin-storeから復元
- DnDとソートが共存し、レーン間移動が正常動作
- 全テスト通過
</success_criteria>

<output>
After completion, create `.planning/phases/07-sort/07-02-SUMMARY.md`
</output>
