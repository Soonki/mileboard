---
phase: 05-drag-drop-with-optimistic-updates
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src-tauri/src/backlog/error.rs
  - src-tauri/src/backlog/client.rs
  - src-tauri/src/backlog/commands.rs
  - src-tauri/src/lib.rs
  - src/types/board.ts
  - src/services/tauriBridge.ts
  - src/services/tauriBridge.test.ts
  - src/stores/boardStore.ts
  - src/stores/boardStore.test.ts
  - src/global.css
  - tests/setup.ts
autonomous: true
requirements:
  - DND-01
  - DND-02
  - DND-03
  - UX-02

must_haves:
  truths:
    - "Rust backend can PATCH a Backlog issue's milestones while preserving non-prefix milestones"
    - "Frontend can call updateIssueMilestone through tauriBridge proxy"
    - "boardStore.moveIssue performs optimistic update with snapshot/rollback on API failure"
    - "Multi-milestone issues are detected and placed in earliest-start-date lane"
    - "Error toast displays Japanese message on API failure"
    - "CSS tokens for DnD visual states are available"
  artifacts:
    - path: "src-tauri/src/backlog/client.rs"
      provides: "update_milestone method with prefix-aware array preservation"
      contains: "async fn update_milestone"
    - path: "src-tauri/src/backlog/commands.rs"
      provides: "update_issue_milestone IPC command"
      contains: "pub async fn update_issue_milestone"
    - path: "src-tauri/src/lib.rs"
      provides: "Command registration"
      contains: "update_issue_milestone"
    - path: "src/services/tauriBridge.ts"
      provides: "updateIssueMilestone IPC proxy"
      exports: ["updateIssueMilestone"]
    - path: "src/stores/boardStore.ts"
      provides: "moveIssue action with optimistic update + rollback"
      contains: "moveIssue"
    - path: "src/types/board.ts"
      provides: "UpdateIssueMilestoneParams type"
      contains: "UpdateIssueMilestoneParams"
    - path: "src/global.css"
      provides: "Phase 5 CSS tokens"
      contains: "--color-drop-target"
  key_links:
    - from: "src/stores/boardStore.ts"
      to: "src/services/tauriBridge.ts"
      via: "updateIssueMilestone call in moveIssue"
      pattern: "updateIssueMilestone"
    - from: "src/services/tauriBridge.ts"
      to: "src-tauri/src/backlog/commands.rs"
      via: "invoke('update_issue_milestone')"
      pattern: "invoke.*update_issue_milestone"
    - from: "src-tauri/src/backlog/commands.rs"
      to: "src-tauri/src/backlog/client.rs"
      via: "client.update_milestone()"
      pattern: "update_milestone"
---

<objective>
Rust backendにBacklog課題のマイルストーン更新コマンドを追加し、TypeScriptのデータ層（tauriBridge、boardStore）にオプティミスティック更新+ロールバックパターンを実装する。

Purpose: DnD UIが依存するデータ更新パイプライン全体を構築する。Rustのマイルストーン配列保持ロジック（最重要データ整合性要件）とフロントエンドのsnapshot/rollbackパターンを、UIコンポーネントとは独立してテスト可能な形で提供する。

Output: テスト済みのRust IPCコマンド、tauriBridge proxy関数、boardStore moveIssueアクション、Phase 5用CSSトークン
</objective>

<execution_context>
@C:\Users\sungi\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\sungi\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-drag-drop-with-optimistic-updates/05-CONTEXT.md
@.planning/phases/05-drag-drop-with-optimistic-updates/05-RESEARCH.md

<interfaces>
<!-- Existing types the executor needs -->

From src/types/backlog.ts:
```typescript
export interface BacklogMilestone {
  id: number;
  projectId: number;
  name: string;
  description: string | null;
  startDate: string | null;
  releaseDueDate: string | null;
  archived: boolean;
  displayOrder: number;
}

export interface BacklogIssue {
  id: number;
  projectId: number;
  issueKey: string;
  keyId: number;
  summary: string;
  description: string | null;
  status: BacklogStatus;
  priority: BacklogPriority | null;
  assignee: BacklogUser | null;
  milestone: BacklogMilestone[];
  category: BacklogCategory[];
  startDate: string | null;
  dueDate: string | null;
  created: string;
  updated: string;
}
```

From src/types/board.ts:
```typescript
export interface MilestoneWithIssues {
  milestone: BacklogMilestone;
  issues: BacklogIssue[];
}

export interface BoardData {
  milestones: MilestoneWithIssues[];
  unassignedIssues: BacklogIssue[];
}

export interface FetchBoardParams {
  host: string;
  apiKey: string;
  projectId: number;
  projectKey: string;
  milestonePrefix: string;
  categoryIds: number[] | null;
}
```

From src/stores/boardStore.ts:
```typescript
type BoardStatus = 'idle' | 'loading' | 'loaded' | 'error';
interface BoardStoreState {
  status: BoardStatus;
  data: BoardData | null;
  error: string | null;
  isReloading: boolean;
  fetchBoard: () => Promise<void>;
  reset: () => void;
}
```

From src/stores/settingsStore.ts:
```typescript
interface SettingsStoreState {
  settings: ConnectionSettings; // { hostUrl, apiKey, projectId, projectKey, milestonePrefix }
  // ...
}
```

From src-tauri/src/backlog/error.rs:
```rust
#[derive(Debug, Error)]
pub enum BacklogError {
    ConnectionFailed(String),
    Unauthorized,
    RateLimited,
    ProjectNotFound(String),
    FetchFailed(String),
    ParseError(String),
    Timeout,
    InvalidInput(String),
}
```

From src-tauri/src/backlog/types.rs:
```rust
pub struct Issue {
    pub id: u64,
    pub issue_key: String,
    pub milestone: Vec<Milestone>,
    // ...
}
pub struct Milestone {
    pub id: u64,
    pub name: String,
    pub start_date: Option<String>,
    // ...
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Rust update_issue_milestone コマンドとtauriBridge proxy</name>
  <files>
    src-tauri/src/backlog/error.rs,
    src-tauri/src/backlog/client.rs,
    src-tauri/src/backlog/commands.rs,
    src-tauri/src/lib.rs,
    src/types/board.ts,
    src/services/tauriBridge.ts,
    src/services/tauriBridge.test.ts
  </files>
  <read_first>
    src-tauri/src/backlog/client.rs,
    src-tauri/src/backlog/commands.rs,
    src-tauri/src/backlog/error.rs,
    src-tauri/src/backlog/types.rs,
    src-tauri/src/lib.rs,
    src/services/tauriBridge.ts,
    src/types/board.ts,
    tests/setup.ts
  </read_first>
  <behavior>
    - Test 1 (Rust client): update_milestone preserves non-prefix milestones (filter logic unit test on milestone array reconstruction)
    - Test 2 (Rust client): update_milestone with new_milestone_id=None produces array without any prefix milestones
    - Test 3 (Rust client): update_milestone with empty milestone_ids sends form param to clear milestones
    - Test 4 (Rust error): UpdateFailed variant produces Japanese error string containing "マイルストーンの更新に失敗しました"
    - Test 5 (tauriBridge): updateIssueMilestone calls invoke with correct command name "update_issue_milestone" and camelCase params
  </behavior>
  <action>
    **Per D-18, D-19:** Rust backendとTypeScript IPC proxyを実装する。

    **1. BacklogError (error.rs):** `UpdateFailed(String)` バリアントを追加。
    ```rust
    #[error("マイルストーンの更新に失敗しました: {0}")]
    UpdateFailed(String),
    ```

    **2. BacklogClient (client.rs):** `fetch_issue` と `update_milestone` メソッドを追加。

    `fetch_issue` メソッド:
    - GET `https://{host}/api/v2/issues/{issue_id_or_key}?apiKey={api_key}`
    - レスポンスを `Issue` にデシリアライズ
    - 既存の `check_response` と `map_reqwest_error` を再利用

    `update_milestone` メソッド (per D-18):
    - パラメータ: `&self, host: &str, api_key: &str, issue_id_or_key: &str, new_milestone_id: Option<u64>, prefix: &str`
    - (1) `fetch_issue` で現在のマイルストーン配列を取得
    - (2) `issue.milestone.iter().filter(|m| !m.name.starts_with(prefix))` で非プレフィックスマイルストーンを保持
    - (3) `new_milestone_id` が Some なら配列に追加。None なら追加しない（unassigned移動）
    - (4) PATCH `https://{host}/api/v2/issues/{issue_id_or_key}?apiKey={api_key}` で `milestoneId[]` を送信
    - 空配列の場合: `milestoneId[]` に空文字列を送信してクリア
    - form bodyの構築: `Vec<(&str, String)>` で `("milestoneId[]", mid.to_string())`
    - `self.http.patch(&url).form(&form_params).send()` → `check_response` → `Ok(())`

    milestone配列再構築ロジックをテスト可能にするため、`rebuild_milestone_ids` をpub(crate)のヘルパー関数として抽出:
    ```rust
    pub(crate) fn rebuild_milestone_ids(
        current_milestones: &[Milestone],
        new_milestone_id: Option<u64>,
        prefix: &str,
    ) -> Vec<u64> {
        let mut ids: Vec<u64> = current_milestones
            .iter()
            .filter(|m| !m.name.starts_with(prefix))
            .map(|m| m.id)
            .collect();
        if let Some(mid) = new_milestone_id {
            ids.push(mid);
        }
        ids
    }
    ```

    **3. commands.rs:** `update_issue_milestone` コマンドを追加。
    ```rust
    #[tauri::command]
    pub async fn update_issue_milestone(
        client: State<'_, BacklogClient>,
        host: String,
        api_key: String,
        issue_id_or_key: String,
        new_milestone_id: Option<u64>,
        milestone_prefix: String,
    ) -> Result<(), String> {
        client
            .update_milestone(&host, &api_key, &issue_id_or_key, new_milestone_id, &milestone_prefix)
            .await
            .map_err(|e| e.to_string())
    }
    ```
    全パラメータは `String` 型（Tauri asyncコマンドのSend制約、既存パターン準拠）。

    **4. lib.rs:** `invoke_handler` に `update_issue_milestone` を追加:
    ```rust
    .invoke_handler(tauri::generate_handler![
        backlog::commands::fetch_board_data,
        backlog::commands::update_issue_milestone
    ])
    ```

    **5. board.ts:** `UpdateIssueMilestoneParams` 型を追加:
    ```typescript
    export interface UpdateIssueMilestoneParams {
      host: string;
      apiKey: string;
      issueIdOrKey: string;
      newMilestoneId: number | null;
      milestonePrefix: string;
    }
    ```

    **6. tauriBridge.ts (per D-19):** `updateIssueMilestone` 関数を追加:
    ```typescript
    export async function updateIssueMilestone(
      host: string,
      apiKey: string,
      issueIdOrKey: string,
      newMilestoneId: number | null,
      milestonePrefix: string,
    ): Promise<void> {
      const params: UpdateIssueMilestoneParams = {
        host, apiKey, issueIdOrKey, newMilestoneId, milestonePrefix,
      };
      return invoke<void>('update_issue_milestone', { ...params });
    }
    ```

    **7. tauriBridge.test.ts:** updateIssueMilestoneが`invoke`を正しいコマンド名・パラメータで呼ぶテストを追加。既存のinvokeモック（tests/setup.ts）を利用。

    **8. Rust tests (client.rs内 #[cfg(test)]):** `rebuild_milestone_ids` のユニットテスト:
    - プレフィックス一致のマイルストーンが除外される
    - 非プレフィックスマイルストーンが保持される
    - new_milestone_id=Noneで空配列になるケース
    - new_milestone_id=Someで正しく追加されるケース
  </action>
  <verify>
    <automated>cd src-tauri && cargo test rebuild_milestone_ids && cd .. && npx vitest run src/services/tauriBridge.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src-tauri/src/backlog/error.rs contains `UpdateFailed(String)` variant
    - src-tauri/src/backlog/error.rs contains `マイルストーンの更新に失敗しました`
    - src-tauri/src/backlog/client.rs contains `pub async fn update_milestone(`
    - src-tauri/src/backlog/client.rs contains `pub(crate) fn rebuild_milestone_ids(`
    - src-tauri/src/backlog/client.rs contains `.filter(|m| !m.name.starts_with(prefix))`
    - src-tauri/src/backlog/client.rs contains `self.http.patch(`
    - src-tauri/src/backlog/client.rs contains `("milestoneId[]"`
    - src-tauri/src/backlog/commands.rs contains `pub async fn update_issue_milestone(`
    - src-tauri/src/lib.rs contains `update_issue_milestone`
    - src/types/board.ts contains `export interface UpdateIssueMilestoneParams`
    - src/services/tauriBridge.ts contains `export async function updateIssueMilestone(`
    - src/services/tauriBridge.ts contains `invoke<void>('update_issue_milestone'`
    - `cd src-tauri && cargo test rebuild_milestone_ids` exits 0
    - `npx vitest run src/services/tauriBridge.test.ts` exits 0
  </acceptance_criteria>
  <done>
    Rust update_milestone メソッドが非プレフィックスマイルストーン保持ロジックを持ち、rebuild_milestone_idsのユニットテストが全パス。update_issue_milestone IPCコマンドが登録済み。tauriBridge.updateIssueMilestoneがinvokeを正しく呼ぶテストがパス。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: boardStore moveIssueアクション + CSSトークン + sonnerモック</name>
  <files>
    src/stores/boardStore.ts,
    src/stores/boardStore.test.ts,
    src/global.css,
    tests/setup.ts
  </files>
  <read_first>
    src/stores/boardStore.ts,
    src/stores/boardStore.test.ts,
    src/stores/settingsStore.ts,
    src/services/tauriBridge.ts,
    src/types/board.ts,
    src/types/backlog.ts,
    src/global.css,
    tests/setup.ts
  </read_first>
  <behavior>
    - Test 1: moveIssue moves issue from source lane to target lane optimistically (data changes immediately before API resolves)
    - Test 2: moveIssue rolls back to snapshot when API call rejects
    - Test 3: moveIssue calls toast.error with Japanese message on API failure
    - Test 4: moveIssue from milestone lane to "unassigned" passes newMilestoneId=null
    - Test 5: moveIssue from "unassigned" to milestone lane passes correct milestoneId
    - Test 6: applyMoveIssue correctly transfers issue between two milestone lanes
    - Test 7: applyMoveIssue correctly transfers issue from unassigned to milestone lane
    - Test 8: applyMoveIssue correctly transfers issue from milestone lane to unassigned
    - Test 9: parseMilestoneIdFromLaneId("unassigned") returns null, parseMilestoneIdFromLaneId("milestone-42") returns 42
    - Test 10: findIssueInBoardData finds issue across all lanes
  </behavior>
  <action>
    **Per D-16, D-17, D-11, D-12, D-13, D-14, D-15:** boardStoreにmoveIssueアクションと関連ヘルパーを実装する。

    **1. boardStore.ts に追加する型とヘルパー関数:**

    laneId規約: `"unassigned"` | `"milestone-{id}"` (文字列統一、Pitfall 6回避)

    ```typescript
    // laneIdからmilestoneIdを抽出。"unassigned"→null、"milestone-42"→42
    export function parseMilestoneIdFromLaneId(laneId: string): number | null {
      if (laneId === 'unassigned') return null;
      const match = laneId.match(/^milestone-(\d+)$/);
      return match ? Number(match[1]) : null;
    }

    // BoardData内の全レーンから issueId で課題を検索
    export function findIssueInBoardData(data: BoardData, issueId: number): BacklogIssue | null {
      const inUnassigned = data.unassignedIssues.find((i) => i.id === issueId);
      if (inUnassigned) return inUnassigned;
      for (const mwi of data.milestones) {
        const found = mwi.issues.find((i) => i.id === issueId);
        if (found) return found;
      }
      return null;
    }

    // オプティミスティック更新: fromLaneからissueを除去し、toLaneに追加（イミュータブル）
    export function applyMoveIssue(
      data: BoardData,
      issueId: number,
      fromLaneId: string,
      toLaneId: string,
    ): BoardData {
      // 移動対象の課題を探す
      let movedIssue: BacklogIssue | null = null;

      // fromLaneから課題を除去
      const removeFromUnassigned = fromLaneId === 'unassigned';
      const newUnassigned = removeFromUnassigned
        ? data.unassignedIssues.filter((i) => {
            if (i.id === issueId) { movedIssue = i; return false; }
            return true;
          })
        : [...data.unassignedIssues];

      const newMilestones = data.milestones.map((mwi) => {
        const msLaneId = `milestone-${mwi.milestone.id}`;
        if (msLaneId === fromLaneId) {
          return {
            ...mwi,
            issues: mwi.issues.filter((i) => {
              if (i.id === issueId) { movedIssue = i; return false; }
              return true;
            }),
          };
        }
        return mwi;
      });

      if (!movedIssue) return data;

      // toLaneに課題を追加
      if (toLaneId === 'unassigned') {
        return {
          milestones: newMilestones,
          unassignedIssues: [...newUnassigned, movedIssue],
        };
      }

      return {
        milestones: newMilestones.map((mwi) => {
          const msLaneId = `milestone-${mwi.milestone.id}`;
          if (msLaneId === toLaneId) {
            return { ...mwi, issues: [...mwi.issues, movedIssue!] };
          }
          return mwi;
        }),
        unassignedIssues: newUnassigned,
      };
    }
    ```

    **2. BoardStoreState interfaceに追加:**
    ```typescript
    moveIssue: (issueId: number, fromLaneId: string, toLaneId: string) => void;
    ```

    **3. moveIssue アクション実装 (per D-16, D-17):**
    ```typescript
    import { toast } from 'sonner';
    import { updateIssueMilestone } from '../services/tauriBridge';

    moveIssue: (issueId, fromLaneId, toLaneId) => {
      const snapshot = get().data;
      if (!snapshot) return;

      // 1. Optimistic update (immutable)
      const updatedData = applyMoveIssue(snapshot, issueId, fromLaneId, toLaneId);
      set({ data: updatedData });

      // 2. API call params
      const newMilestoneId = parseMilestoneIdFromLaneId(toLaneId);
      const issue = findIssueInBoardData(snapshot, issueId);
      if (!issue) return;

      const { settings } = useSettingsStore.getState();

      // 3. Async API call — fire-and-forget with catch for rollback (per D-12)
      updateIssueMilestone(
        settings.hostUrl,
        settings.apiKey,
        issue.issueKey,
        newMilestoneId,
        settings.milestonePrefix,
      ).catch((error: unknown) => {
        // 4. Rollback (per D-12: rollback before/simultaneously with toast)
        set({ data: snapshot });
        const message = typeof error === 'string'
          ? `マイルストーンの変更に失敗しました: ${error}`
          : 'マイルストーンの変更に失敗しました';
        toast.error(message);
      });
    },
    ```

    **4. global.css にPhase 5 CSSトークンを追加 (UI-SPEC準拠):**
    `:root` ブロックの末尾（`--transition-normal` の後）に追加:
    ```css
    /* Phase 5: Drag & Drop */
    --color-drop-target: rgba(37, 99, 235, 0.06);
    --shadow-drag: 0 8px 24px rgba(0, 0, 0, 0.25), 0 2px 8px rgba(0, 0, 0, 0.15);
    --transition-drag: 200ms cubic-bezier(0.2, 0, 0, 1);
    ```

    **5. tests/setup.ts にsonnerモックを追加:**
    ```typescript
    vi.mock('sonner', () => ({
      toast: {
        error: vi.fn(),
        success: vi.fn(),
      },
      Toaster: () => null,
    }));
    ```

    **6. boardStore.test.ts にmoveIssue関連テストを追加:**
    - テストデータファクトリ: `makeBoardData`, `makeIssue`, `makeMilestone` ヘルパー
    - vitest の `vi.mocked` で `updateIssueMilestone` をモック
    - 各テストで store を `useBoardStore.setState()` でリセット
    - ヘルパー関数のユニットテスト: `parseMilestoneIdFromLaneId`, `findIssueInBoardData`, `applyMoveIssue`
    - moveIssue統合テスト: 成功時のデータ更新、失敗時のロールバック、toast.error呼び出し
  </action>
  <verify>
    <automated>npx vitest run src/stores/boardStore.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/stores/boardStore.ts contains `export function parseMilestoneIdFromLaneId(`
    - src/stores/boardStore.ts contains `export function findIssueInBoardData(`
    - src/stores/boardStore.ts contains `export function applyMoveIssue(`
    - src/stores/boardStore.ts contains `moveIssue:` action in store
    - src/stores/boardStore.ts contains `import { toast } from 'sonner'`
    - src/stores/boardStore.ts contains `import { updateIssueMilestone } from`
    - src/stores/boardStore.ts contains `set({ data: snapshot })` (rollback)
    - src/stores/boardStore.ts contains `toast.error(message)` 
    - src/stores/boardStore.ts contains `マイルストーンの変更に失敗しました`
    - src/global.css contains `--color-drop-target: rgba(37, 99, 235, 0.06)`
    - src/global.css contains `--shadow-drag:`
    - src/global.css contains `--transition-drag:`
    - tests/setup.ts contains `vi.mock('sonner'`
    - `npx vitest run src/stores/boardStore.test.ts` exits 0 with 10+ tests passing
  </acceptance_criteria>
  <done>
    boardStore.moveIssueがオプティミスティック更新+ロールバック+toast.errorを正しく行い、全ヘルパー関数のユニットテストがパス。Phase 5 CSSトークンが global.css に定義済み。sonnerモックが tests/setup.ts に追加済み。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Frontend -> Rust IPC | issueIdOrKey, newMilestoneId, milestonePrefix がフロントエンドから送信される |
| Rust -> Backlog API | APIキーを含むHTTPリクエストが外部APIに送信される |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | update_issue_milestone | mitigate | Backlog APIサーバー側で権限チェック。Rust側で issue_id_or_key が空でないことを検証 (validate_params パターン) |
| T-05-02 | Information Disclosure | client.rs PATCH URL | mitigate | APIキーはURLクエリパラメータのみ（Backlog API仕様）。エラーメッセージにAPIキーを含めない。既存 error_messages_do_not_contain_api_key テストパターン踏襲 |
| T-05-03 | Denial of Service | PATCH呼び出し頻度 | mitigate | 既存の throttle_if_needed + check_response で429レート制限対応。PATCHも同じBacklogClientインスタンス経由 |
| T-05-04 | Tampering | boardStore optimistic update | accept | フロントエンドのローカル状態のみ。API失敗時にロールバックするため永続的なデータ破損なし |
| T-05-05 | Information Disclosure | toast error messages | accept | エラーメッセージは日本語のBacklogError文字列のみ。内部パスやスタックトレースは含まれない |
</threat_model>

<verification>
1. `cd src-tauri && cargo test` -- Rust全テストパス（rebuild_milestone_ids含む）
2. `npx vitest run src/stores/boardStore.test.ts` -- boardStore moveIssue全テストパス
3. `npx vitest run src/services/tauriBridge.test.ts` -- tauriBridge updateIssueMilestoneテストパス
4. `npx vitest run` -- 全フロントエンドテストパス（既存テスト含む回帰なし）
5. src/global.css に Phase 5 CSSトークン3つが存在
</verification>

<success_criteria>
- Rust backend: update_milestone が非プレフィックスマイルストーンを正しく保持する（ユニットテスト証明）
- IPC: update_issue_milestone コマンドが lib.rs に登録済み
- tauriBridge: updateIssueMilestone が invoke を正しいコマンド名で呼ぶ（テスト証明）
- boardStore: moveIssue がオプティミスティック更新→API呼び出し→失敗時ロールバック+toast.error（テスト証明）
- CSS: --color-drop-target, --shadow-drag, --transition-drag が global.css に定義済み
- 全テストスイート（既存+新規）がグリーン
</success_criteria>

<output>
After completion, create `.planning/phases/05-drag-drop-with-optimistic-updates/05-01-SUMMARY.md`
</output>
