---
phase: 05-drag-drop-with-optimistic-updates
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified: []
autonomous: false
requirements:
  - DND-01
  - DND-02
  - DND-03
  - UX-02

must_haves:
  truths:
    - "Dragging a card between milestone lanes visually moves the card"
    - "Dragged card appears opaque with scale-up and shadow"
    - "Original card position disappears during drag"
    - "Drop target lane gets subtle background highlight"
    - "Card clicks still open Backlog URL (not intercepted by drag)"
    - "Multi-milestone cards show warning badge with tooltip on hover"
    - "Multi-milestone cards show not-allowed cursor and cannot be dragged"
    - "Error toast appears at bottom-right when API fails"
  artifacts: []
  key_links: []
---

<objective>
Phase 5のDnD実装を視覚的・機能的に検証する。

Purpose: 自動テストではカバーできないCSS視覚効果、DnDインタラクション、sonnerトースト表示を人間が確認する。

Output: Phase 5完了の承認または修正指示
</objective>

<execution_context>
@C:\Users\sungi\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\sungi\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/phases/05-drag-drop-with-optimistic-updates/05-CONTEXT.md
@.planning/phases/05-drag-drop-with-optimistic-updates/05-UI-SPEC.md
@.planning/phases/05-drag-drop-with-optimistic-updates/05-01-SUMMARY.md
@.planning/phases/05-drag-drop-with-optimistic-updates/05-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: 最終回帰テスト + ビルド確認</name>
  <files></files>
  <read_first>
    src/components/Board/Board.tsx,
    src/stores/boardStore.ts
  </read_first>
  <action>
    全テストスイートとビルドを実行して回帰がないことを確認する。

    1. `npx vitest run` -- フロントエンド全テスト
    2. `cd src-tauri && cargo test` -- Rust全テスト
    3. `npm run build` -- フロントエンドビルド
    4. `cd src-tauri && cargo build` -- Rustビルド

    全コマンドがエラーなく完了すること。テスト失敗やビルドエラーがあれば修正する。
  </action>
  <verify>
    <automated>npx vitest run && cd src-tauri && cargo test && cd .. && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run` exits 0
    - `cd src-tauri && cargo test` exits 0
    - `npm run build` exits 0
  </acceptance_criteria>
  <done>
    全テスト・全ビルドがグリーン。Phase 5の全コードが回帰なく統合されている。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 5 DnD機能の視覚・機能検証</name>
  <files></files>
  <action>
    `npm run tauri dev` でアプリを起動し、Phase 5のDnD機能を視覚的・機能的に検証する。
    以下の全項目を確認し、問題があれば修正する。
  </action>
  <verify>
    <automated>echo "checkpoint:human-verify -- manual verification required"</automated>
  </verify>
  <done>
    人間が全DnDインタラクションを視覚的に確認し "approved" と回答。
  </done>
  <what-built>
    Phase 5 ドラッグ&ドロップ機能の全実装:
    - カードのレーン間ドラッグ&ドロップ（マイルストーン移動）
    - オプティミスティック更新 + API失敗時ロールバック
    - マルチマイルストーン警告バッジとツールチップ
    - ドラッグオーバーレイ（scale 1.05 + 大きなドロップシャドウ）
    - ドロップターゲットレーンのハイライト
    - エラートースト（sonner、右下表示）
    - 未割り当てレーンとの双方向DnD
  </what-built>
  <how-to-verify>
    `npm run tauri dev` でアプリを起動し、以下を確認:

    **1. 基本DnD (DND-01):**
    - [ ] カードをクリックしてドラッグすると、カードが持ち上がる（scale 1.05 + 大きなシャドウ）
    - [ ] ドラッグ中、元の位置のカードは非表示になる
    - [ ] 別のマイルストーンレーンにドロップするとカードが移動する
    - [ ] ドロップ先レーンにホバーすると薄い青色のハイライトが表示される

    **2. クリックとの共存 (D-04):**
    - [ ] カードをクリック（ドラッグせず）するとBacklog URLがブラウザで開く
    - [ ] 5px以内のマウス移動はクリックとして処理される

    **3. オプティミスティック更新 (DND-02):**
    - [ ] ドロップ直後にカードが即座にターゲットレーンに表示される（API応答を待たない）
    - [ ] レーンヘッダーの課題数が即座に更新される

    **4. 未割り当てレーン DnD (D-13, D-14):**
    - [ ] 未割り当てレーンからマイルストーンレーンにカードをドラッグできる
    - [ ] マイルストーンレーンから未割り当てレーンにカードをドラッグできる

    **5. マルチマイルストーン (DND-03, D-05, D-06, D-08):**
    ※ マルチマイルストーンの課題がある場合のみ確認:
    - [ ] 複数マイルストーンに属するカードに警告アイコンが表示される
    - [ ] 警告アイコンにマウスホバーすると他のマイルストーン名がツールチップ表示される
    - [ ] マルチマイルストーンカードはドラッグできない（not-allowedカーソル）

    **6. エラートースト (UX-02, D-09, D-10):**
    ※ API失敗を確認するには:
    - ネットワーク切断、または不正な設定でテスト
    - [ ] 失敗時にカードが元のレーンに戻る（ロールバック）
    - [ ] 右下にエラートーストが表示される
    - [ ] 5秒後に自動で消える
    - [ ] 閉じるボタンで手動で消せる
  </how-to-verify>
  <resume-signal>"approved" と入力するか、修正が必要な箇所を説明してください</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| N/A | 検証フェーズ -- 新しいコードなし |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| N/A | N/A | N/A | N/A | 検証のみ -- Plan 01/02 で対応済み |
</threat_model>

<verification>
1. 全テスト・ビルドがグリーン（Task 1自動検証）
2. 人間による視覚・機能検証（Task 2チェックポイント）
</verification>

<success_criteria>
- 全テストスイート・全ビルドがエラーなく完了
- 人間が全DnDインタラクションを視覚的に確認し "approved" と回答
</success_criteria>

<output>
After completion, create `.planning/phases/05-drag-drop-with-optimistic-updates/05-03-SUMMARY.md`
</output>
