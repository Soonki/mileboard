---
phase: 08-reorder
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/reorder.ts
  - src/utils/reorderUtils.ts
  - src/utils/reorderUtils.test.ts
  - src/services/reorderStorage.ts
  - src/services/reorderStorage.test.ts
  - src/stores/reorderStore.ts
  - src/stores/reorderStore.test.ts
autonomous: true
requirements: [REORD-01, REORD-02]

must_haves:
  truths:
    - "ReorderMap 型が Record<string, number[]> として定義されている"
    - "applyCustomOrder がカスタム順序どおりに課題を並び替える"
    - "カスタム順序なしの場合は keyId 昇順で返す（D-02）"
    - "新規課題（savedIds にないもの）は末尾に keyId 昇順で追加される（D-04）"
    - "reorderStorage が plugin-store に ReorderMap を保存・読み込みできる"
    - "reorderStore.reorder() が orderMap を arrayMove で更新し即時保存する"
    - "reorderStore.updateOnCrossLaneMove() が移動元から削除し移動先末尾に追加する（D-07, D-08）"
    - "reorderStore.loadFromStorage() が plugin-store から orderMap を復元する"
  artifacts:
    - path: "src/types/reorder.ts"
      provides: "ReorderMap 型定義"
      contains: "export type ReorderMap"
    - path: "src/utils/reorderUtils.ts"
      provides: "applyCustomOrder 純粋関数"
      exports: ["applyCustomOrder"]
    - path: "src/services/reorderStorage.ts"
      provides: "plugin-store load/save 関数"
      exports: ["loadReorderConfig", "saveReorderConfig"]
    - path: "src/stores/reorderStore.ts"
      provides: "Zustand reorderStore"
      exports: ["useReorderStore"]
  key_links:
    - from: "src/stores/reorderStore.ts"
      to: "src/services/reorderStorage.ts"
      via: "saveReorderConfig / loadReorderConfig 呼び出し"
      pattern: "saveReorderConfig|loadReorderConfig"
    - from: "src/stores/reorderStore.ts"
      to: "src/types/reorder.ts"
      via: "ReorderMap 型インポート"
      pattern: "import.*ReorderMap.*from"
    - from: "src/utils/reorderUtils.ts"
      to: "src/types/backlog.ts"
      via: "BacklogIssue 型インポート"
      pattern: "import.*BacklogIssue.*from"
---

<objective>
レーン内並べ替え機能の型定義、純粋関数、永続化サービス、Zustand ストアを TDD で構築する。

Purpose: Plan 02 での Board.tsx / IssueCard.tsx 統合に必要なすべてのインフラストラクチャを提供する。sortStore / sortStorage と同一パターンで構築し、コードベースの一貫性を維持する。

Output: src/types/reorder.ts, src/utils/reorderUtils.ts, src/services/reorderStorage.ts, src/stores/reorderStore.ts + 対応するテストファイル3つ
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/08-reorder/08-CONTEXT.md
@.planning/phases/08-reorder/08-RESEARCH.md
@.planning/phases/08-reorder/08-UI-SPEC.md

<interfaces>
<!-- 既存コードからの型定義・パターン。executor はこれを直接参照すること。 -->

From src/types/backlog.ts:
```typescript
export interface BacklogIssue {
  id: number;
  projectId: number;
  issueKey: string;
  keyId: number;
  summary: string;
  // ... 他フィールド省略
}
```

From src/types/sort.ts (reorder.ts のパターン参考):
```typescript
export type SortField = 'none' | 'assignee' | 'dueDate';
export type SortDirection = 'asc' | 'desc';
export interface SortConfig {
  field: SortField;
  direction: SortDirection;
}
```

From src/services/sortStorage.ts (reorderStorage.ts のパターン参考):
```typescript
import { load } from '@tauri-apps/plugin-store';
const STORE_FILE = 'settings.json';
const SORT_KEY = 'sort';
export async function loadSortConfig(): Promise<SortConfig | null> { ... }
export async function saveSortConfig(config: SortConfig): Promise<void> { ... }
```

From src/stores/sortStore.ts (reorderStore.ts のパターン参考):
```typescript
import { create } from 'zustand';
export const useSortStore = create<SortStoreState>()((set, get) => ({
  field: 'none',
  direction: 'asc',
  setField: (field) => { set({ field }); saveSortConfig({ ... }); },
  loadFromStorage: async () => { const config = await loadSortConfig(); if (config) set({ ... }); },
}));
```

From src/utils/sortUtils.ts (reorderUtils.ts のパターン参考):
```typescript
export function applySortToIssues(
  issues: ReadonlyArray<BacklogIssue>,
  field: SortField,
  direction: SortDirection,
): BacklogIssue[] { ... }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ReorderMap 型定義 + applyCustomOrder 純粋関数</name>
  <files>src/types/reorder.ts, src/utils/reorderUtils.ts, src/utils/reorderUtils.test.ts</files>
  <read_first>
    - src/types/sort.ts (型定義パターン参考)
    - src/utils/sortUtils.ts (純粋関数パターン参考)
    - src/types/backlog.ts (BacklogIssue 型)
  </read_first>
  <behavior>
    - applyCustomOrder([], []) -> []（空配列）
    - applyCustomOrder(issues, []) -> keyId昇順にソートされた配列（D-02: カスタム順序なし）
    - applyCustomOrder([id=3,id=1,id=2], [2,3,1]) -> [id=2, id=3, id=1]（カスタム順序適用）
    - applyCustomOrder([id=1,id=2,id=3], [3,1]) -> [id=3, id=1, id=2]（D-04: id=2は末尾に追加）
    - applyCustomOrder([id=1,id=3], [3,2,1]) -> [id=3, id=1]（削除済みid=2は除外）
    - applyCustomOrder は入力配列を mutate しない（immutability）
    - 新規課題が複数の場合、keyId昇順で末尾に追加（D-04）
  </behavior>
  <action>
1. src/types/reorder.ts を作成:
   ```typescript
   /** レーンIDから課題ID配列へのマッピング。カスタム並び順を表す。 */
   export type ReorderMap = Record<string, number[]>;
   ```

2. src/utils/reorderUtils.ts を作成:
   - `applyCustomOrder(issues: ReadonlyArray<BacklogIssue>, savedIds: number[]): BacklogIssue[]`
   - savedIds.length === 0 の場合: `[...issues].sort((a, b) => a.keyId - b.keyId)` を返す（D-02）
   - savedIds.length > 0 の場合:
     - `issueMap = new Map(issues.map(i => [i.id, i]))`
     - `ordered = savedIds.flatMap(id => { const issue = issueMap.get(id); return issue ? [issue] : []; })`
     - `savedSet = new Set(savedIds)`
     - `newIssues = [...issues].filter(i => !savedSet.has(i.id)).sort((a, b) => a.keyId - b.keyId)`
     - return `[...ordered, ...newIssues]`（D-04）

3. src/utils/reorderUtils.test.ts を作成:
   - behavior ブロックの全ケースをテスト
   - テストヘルパー: `makeIssue(id: number, keyId: number): BacklogIssue` を定義（他フィールドはダミー値）
  </action>
  <verify>
    <automated>npx vitest run src/utils/reorderUtils.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/types/reorder.ts に `export type ReorderMap = Record<string, number[]>` が存在する
    - src/utils/reorderUtils.ts に `export function applyCustomOrder(` が存在する
    - src/utils/reorderUtils.ts が `ReadonlyArray<BacklogIssue>` を引数に取る
    - src/utils/reorderUtils.test.ts が 7 件以上のテストケースを含む
    - `npx vitest run src/utils/reorderUtils.test.ts` が全件パスする
  </acceptance_criteria>
  <done>ReorderMap 型と applyCustomOrder 純粋関数が TDD で実装済み。カスタム順序適用、keyId フォールバック（D-02）、新規課題末尾追加（D-04）、削除済み課題除外がすべてテスト済み。</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: reorderStorage 永続化サービス + useReorderStore Zustand ストア</name>
  <files>src/services/reorderStorage.ts, src/services/reorderStorage.test.ts, src/stores/reorderStore.ts, src/stores/reorderStore.test.ts</files>
  <read_first>
    - src/services/sortStorage.ts (plugin-store load/save パターン — 完全に模倣する)
    - src/stores/sortStore.ts (Zustand store パターン — 完全に模倣する)
    - src/types/reorder.ts (Task 1 で作成した ReorderMap 型)
    - tests/setup.ts (Tauri モックのセットアップ確認)
  </read_first>
  <behavior>
    reorderStorage:
    - loadReorderConfig() が plugin-store から ReorderMap を返す
    - loadReorderConfig() がデータなしの場合 null を返す
    - loadReorderConfig() が不正データ（非オブジェクト、値が非number[]）の場合 null を返す（T-08-01 バリデーション）
    - saveReorderConfig(orderMap) が store.set('reorder', orderMap) + store.save() を呼ぶ

    reorderStore:
    - 初期状態: orderMap = {}
    - reorder(laneId, activeId, overId): orderMap[laneId] 内で activeId の位置を overId の位置に arrayMove して即時保存
    - setLaneOrder(laneId, issueIds): orderMap[laneId] を issueIds で置き換えて即時保存
    - removeLaneOrder(laneId): orderMap から laneId を削除して即時保存
    - updateOnCrossLaneMove(issueId, fromLaneId, toLaneId): fromLaneId の orderMap から issueId を除去、toLaneId の orderMap 末尾に issueId を追加して即時保存（D-07, D-08）
    - loadFromStorage(): plugin-store から orderMap を復元
    - loadFromStorage() でデータなしの場合 orderMap を {} のまま維持
  </behavior>
  <action>
1. src/services/reorderStorage.ts を作成（sortStorage.ts と同パターン）:
   ```typescript
   import { load } from '@tauri-apps/plugin-store';
   import type { ReorderMap } from '../types/reorder';

   const STORE_FILE = 'settings.json';
   const REORDER_KEY = 'reorder';

   export async function loadReorderConfig(): Promise<ReorderMap | null> {
     const store = await load(STORE_FILE, { autoSave: false });
     const data = await store.get<ReorderMap>(REORDER_KEY);
     if (!data || typeof data !== 'object' || Array.isArray(data)) return null;
     // T-08-01: バリデーション — 各値が number[] であることを確認
     for (const [, value] of Object.entries(data)) {
       if (!Array.isArray(value) || !value.every((v) => typeof v === 'number')) {
         return null;
       }
     }
     return data;
   }

   export async function saveReorderConfig(orderMap: ReorderMap): Promise<void> {
     const store = await load(STORE_FILE, { autoSave: false });
     await store.set(REORDER_KEY, orderMap);
     await store.save();
   }
   ```

2. src/stores/reorderStore.ts を作成（sortStore.ts と同パターン）:
   ```typescript
   import { create } from 'zustand';
   import { arrayMove } from '@dnd-kit/sortable';
   import type { ReorderMap } from '../types/reorder';
   import { loadReorderConfig, saveReorderConfig } from '../services/reorderStorage';

   interface ReorderStoreState {
     orderMap: ReorderMap;
     reorder: (laneId: string, activeId: number, overId: number) => void;
     setLaneOrder: (laneId: string, issueIds: number[]) => void;
     removeLaneOrder: (laneId: string) => void;
     updateOnCrossLaneMove: (issueId: number, fromLaneId: string, toLaneId: string) => void;
     loadFromStorage: () => Promise<void>;
   }

   export const useReorderStore = create<ReorderStoreState>()((set, get) => ({
     orderMap: {},

     reorder: (laneId, activeId, overId) => {
       const { orderMap } = get();
       const currentOrder = orderMap[laneId] ?? [];
       const fromIndex = currentOrder.indexOf(activeId);
       const toIndex = currentOrder.indexOf(overId);
       if (fromIndex === -1 || toIndex === -1) return;
       const newOrder = arrayMove(currentOrder, fromIndex, toIndex);
       const newMap = { ...orderMap, [laneId]: newOrder };
       set({ orderMap: newMap });
       saveReorderConfig(newMap).catch(() => {});
     },

     setLaneOrder: (laneId, issueIds) => {
       const { orderMap } = get();
       const newMap = { ...orderMap, [laneId]: issueIds };
       set({ orderMap: newMap });
       saveReorderConfig(newMap).catch(() => {});
     },

     removeLaneOrder: (laneId) => {
       const { orderMap } = get();
       const { [laneId]: _, ...rest } = orderMap;
       set({ orderMap: rest });
       saveReorderConfig(rest).catch(() => {});
     },

     updateOnCrossLaneMove: (issueId, fromLaneId, toLaneId) => {
       const { orderMap } = get();
       const fromOrder = (orderMap[fromLaneId] ?? []).filter(id => id !== issueId);
       const toOrder = [...(orderMap[toLaneId] ?? []), issueId];
       const newMap = { ...orderMap, [fromLaneId]: fromOrder, [toLaneId]: toOrder };
       set({ orderMap: newMap });
       saveReorderConfig(newMap).catch(() => {});
     },

     loadFromStorage: async () => {
       const config = await loadReorderConfig();
       if (config) {
         set({ orderMap: config });
       }
     },
   }));
   ```

   **重要:**
   - `reorder` の引数はインデックスではなく `activeId` / `overId`（issueId）。store 内部で orderMap から indexOf で位置を解決する。これにより Pitfall 2（フィルタ中のインデックスずれ）を回避する
   - `saveReorderConfig` は fire-and-forget（`.catch(() => {})` で非クリティカルエラーを握り潰す）。sortStore と同パターン
   - 全 set() 呼び出しでスプレッド構文による新オブジェクト生成（CLAUDE.md immutability 規約）

3. src/services/reorderStorage.test.ts を作成:
   - loadReorderConfig: データあり → ReorderMap を返す
   - loadReorderConfig: データなし → null
   - loadReorderConfig: 不正データ（値が string[]）→ null
   - loadReorderConfig: null → null
   - saveReorderConfig: store.set + store.save が呼ばれる

4. src/stores/reorderStore.test.ts を作成:
   - 初期状態: orderMap === {}
   - reorder: 正常ケース（activeId/overId が orderMap に存在）
   - reorder: activeId が orderMap にない場合は何もしない
   - setLaneOrder: orderMap[laneId] が更新される
   - removeLaneOrder: orderMap から laneId が削除される
   - updateOnCrossLaneMove: fromLane から削除、toLane 末尾に追加（D-07, D-08）
   - updateOnCrossLaneMove: toLane に既存 orderMap がない場合も動作する
   - loadFromStorage: 正常ケース
   - loadFromStorage: null の場合 orderMap を {} のまま維持
  </action>
  <verify>
    <automated>npx vitest run src/services/reorderStorage.test.ts src/stores/reorderStore.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/services/reorderStorage.ts に `export async function loadReorderConfig(` が存在する
    - src/services/reorderStorage.ts に `export async function saveReorderConfig(` が存在する
    - src/services/reorderStorage.ts に `typeof data !== 'object'` バリデーションが存在する（T-08-01）
    - src/stores/reorderStore.ts に `export const useReorderStore` が存在する
    - src/stores/reorderStore.ts に `arrayMove` の import が存在する
    - src/stores/reorderStore.ts の `reorder` が `activeId` と `overId` を引数に取る（インデックスではなくID）
    - src/stores/reorderStore.ts に `updateOnCrossLaneMove` メソッドが存在する
    - src/stores/reorderStore.test.ts に `updateOnCrossLaneMove` のテストが存在する
    - `npx vitest run src/services/reorderStorage.test.ts src/stores/reorderStore.test.ts` が全件パスする
  </acceptance_criteria>
  <done>reorderStorage（plugin-store 永続化）と useReorderStore（Zustand ストア）が TDD で実装済み。reorder は issueId ベースの引数で Pitfall 2 を回避。updateOnCrossLaneMove が D-07/D-08 を実装。loadFromStorage が REORD-02 を実装。全テストパス。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin-store -> reorderStore | plugin-store から読み込んだデータが ReorderMap 形式であることを保証する必要がある |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | reorderStorage.ts loadReorderConfig | mitigate | `typeof data !== 'object'` + `Array.isArray(data)` チェック + 各値の `Array.isArray(value)` + `value.every(v => typeof v === 'number')` バリデーション。不正データは null を返しデフォルト値にフォールバック |
| T-08-02 | Denial of Service | reorderStore.ts reorder | accept | ローカルアプリのため外部攻撃ベクタなし。arrayMove は O(n) で配列サイズは画面内課題数（数十件）に限定 |
</threat_model>

<verification>
```bash
# 全テストが通ること
npx vitest run src/types/reorder.ts src/utils/reorderUtils.test.ts src/services/reorderStorage.test.ts src/stores/reorderStore.test.ts

# 型定義の存在確認
grep "export type ReorderMap" src/types/reorder.ts

# 純粋関数の存在確認
grep "export function applyCustomOrder" src/utils/reorderUtils.ts

# ストアの存在確認
grep "export const useReorderStore" src/stores/reorderStore.ts

# 永続化サービスの存在確認
grep "export async function loadReorderConfig" src/services/reorderStorage.ts
grep "export async function saveReorderConfig" src/services/reorderStorage.ts
```
</verification>

<success_criteria>
- src/types/reorder.ts が ReorderMap 型を export している
- src/utils/reorderUtils.ts が applyCustomOrder 関数を export している
- src/services/reorderStorage.ts が loadReorderConfig / saveReorderConfig を export している
- src/stores/reorderStore.ts が useReorderStore を export している
- reorderUtils.test.ts の全テスト（7件以上）がパス
- reorderStorage.test.ts の全テスト（5件以上）がパス
- reorderStore.test.ts の全テスト（9件以上）がパス
- `npx vitest run` フルスイートがリグレッションなしで全件パス
</success_criteria>

<output>
After completion, create `.planning/phases/08-reorder/08-01-SUMMARY.md`
</output>
