---
phase: 08-reorder
plan: 02
type: execute
wave: 2
depends_on: [08-01]
files_modified:
  - src/components/Board/Board.tsx
  - src/components/Board/Board.test.tsx
  - src/components/IssueCard/IssueCard.tsx
  - src/components/IssueCard/IssueCard.test.tsx
  - src/App.tsx
autonomous: false
requirements: [REORD-01, REORD-02, REORD-03]

must_haves:
  truths:
    - "レーン内でカードをドラッグして並び順を変更できる（REORD-01）"
    - "カスタム並び順がアプリ再起動後も保持される（REORD-02）"
    - "ソートモード中はレーン内並べ替えが無効で、カーソルが grab から default に変わる（REORD-03, D-05）"
    - "ソートモード中もレーン間DnD（マイルストーン移動）は有効のまま（D-06）"
    - "ソートなしに戻すとカスタム順序が復帰する（D-01）"
    - "レーン間移動で移動元の orderMap から削除、移動先の末尾に追加される（D-07, D-08）"
  artifacts:
    - path: "src/components/Board/Board.tsx"
      provides: "handleDragEnd 同一レーン検出 + filteredAndSortedView カスタム順序パイプライン"
      contains: "reorder"
    - path: "src/components/IssueCard/IssueCard.tsx"
      provides: "useSortable disabled 条件にソートモード判定追加"
      contains: "isSortActive"
    - path: "src/App.tsx"
      provides: "reorderStore 起動時復元"
      contains: "useReorderStore"
  key_links:
    - from: "src/components/Board/Board.tsx"
      to: "src/stores/reorderStore.ts"
      via: "useReorderStore import + reorder/updateOnCrossLaneMove 呼び出し"
      pattern: "useReorderStore"
    - from: "src/components/Board/Board.tsx"
      to: "src/utils/reorderUtils.ts"
      via: "applyCustomOrder import + filteredAndSortedView 内での呼び出し"
      pattern: "applyCustomOrder"
    - from: "src/components/IssueCard/IssueCard.tsx"
      to: "src/stores/sortStore.ts"
      via: "useSortStore でソートモード検出"
      pattern: "useSortStore.*field"
    - from: "src/App.tsx"
      to: "src/stores/reorderStore.ts"
      via: "loadFromStorage 起動時呼び出し"
      pattern: "loadReorderFromStorage"
---

<objective>
Board.tsx の handleDragEnd にレーン内並べ替えロジックを追加し、filteredAndSortedView パイプラインにカスタム順序を統合する。IssueCard.tsx にソートモード検出を追加し、App.tsx に reorderStore の起動時復元を追加する。

Purpose: Plan 01 で構築したインフラストラクチャを既存コンポーネントに接続し、REORD-01/02/03 の全要件を完結させる。

Output: Board.tsx, IssueCard.tsx, App.tsx の変更 + 対応テスト更新 + 人間による動作確認
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/REQUIREMENTS.md
@.planning/phases/08-reorder/08-CONTEXT.md
@.planning/phases/08-reorder/08-RESEARCH.md
@.planning/phases/08-reorder/08-UI-SPEC.md
@.planning/phases/08-reorder/08-01-SUMMARY.md

<interfaces>
<!-- Plan 01 で作成された型・関数。executor は直接使用すること。 -->

From src/types/reorder.ts:
```typescript
export type ReorderMap = Record<string, number[]>; // laneId -> issueId[]
```

From src/utils/reorderUtils.ts:
```typescript
export function applyCustomOrder(
  issues: ReadonlyArray<BacklogIssue>,
  savedIds: number[],
): BacklogIssue[];
```

From src/services/reorderStorage.ts:
```typescript
export async function loadReorderConfig(): Promise<ReorderMap | null>;
export async function saveReorderConfig(orderMap: ReorderMap): Promise<void>;
```

From src/stores/reorderStore.ts:
```typescript
interface ReorderStoreState {
  orderMap: ReorderMap;
  reorder: (laneId: string, activeId: number, overId: number) => void;
  setLaneOrder: (laneId: string, issueIds: number[]) => void;
  removeLaneOrder: (laneId: string) => void;
  updateOnCrossLaneMove: (issueId: number, fromLaneId: string, toLaneId: string) => void;
  loadFromStorage: () => Promise<void>;
}
export const useReorderStore: UseBoundStore<StoreApi<ReorderStoreState>>;
```

<!-- 既存コードの主要シグネチャ -->

From src/stores/sortStore.ts:
```typescript
interface SortStoreState {
  field: SortField;       // 'none' | 'assignee' | 'dueDate'
  direction: SortDirection;
  // ...
}
export const useSortStore: UseBoundStore<StoreApi<SortStoreState>>;
```

From src/components/Board/Board.tsx (変更対象):
```typescript
function findLaneContaining(data: BoardData, itemId: number | string): string | null;
function resolveOverLaneId(data: BoardData, overId: number | string): string | null;
// handleDragEnd: 現在は fromLaneId !== toLaneId の場合のみ moveIssue を呼ぶ
// filteredAndSortedView: useMemo で applyFilters -> applySortToIssues パイプライン
```

From src/components/IssueCard/IssueCard.tsx (変更対象):
```typescript
// useSortable({ id: issue.id, disabled: isMultiMilestone })
// -> disabled 条件に isSortActive を追加する
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: App.tsx に reorderStore 起動時復元を追加</name>
  <files>src/App.tsx</files>
  <read_first>
    - src/App.tsx (現在の起動時初期化パターン -- settingsStore + sortStore の loadFromStorage)
    - src/stores/reorderStore.ts (Plan 01 で作成した useReorderStore)
  </read_first>
  <action>
src/App.tsx に以下の変更を加える:

1. import 追加:
   ```typescript
   import { useReorderStore } from './stores/reorderStore';
   ```

2. App コンポーネント内、既存の `loadSortFromStorage` セレクタの直下に追加:
   ```typescript
   const loadReorderFromStorage = useReorderStore((s) => s.loadFromStorage);
   ```

3. 既存の sortStore useEffect の直下に新しい useEffect を追加:
   ```typescript
   useEffect(() => {
     loadReorderFromStorage();
   }, [loadReorderFromStorage]);
   ```

変更後の App.tsx の上部（import + hooks 部分）は以下の構造になる:
```typescript
import { useEffect, useState } from 'react';
import { useSettingsStore } from './stores/settingsStore';
import { useSortStore } from './stores/sortStore';
import { useReorderStore } from './stores/reorderStore';
// ... 他の import は変更なし

function App() {
  const isConfigured = useSettingsStore((s) => s.isConfigured);
  const loadFromStorage = useSettingsStore((s) => s.loadFromStorage);
  const loadSortFromStorage = useSortStore((s) => s.loadFromStorage);
  const loadReorderFromStorage = useReorderStore((s) => s.loadFromStorage);
  const [showSettings, setShowSettings] = useState(false);

  useEffect(() => { loadFromStorage(); }, [loadFromStorage]);
  useEffect(() => { loadSortFromStorage(); }, [loadSortFromStorage]);
  useEffect(() => { loadReorderFromStorage(); }, [loadReorderFromStorage]);
  // ... 残りは変更なし
}
```
  </action>
  <verify>
    <automated>npx vitest run src/App.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/App.tsx に `import { useReorderStore } from './stores/reorderStore'` が存在する
    - src/App.tsx に `loadReorderFromStorage` の変数宣言が存在する
    - src/App.tsx に `loadReorderFromStorage()` を呼ぶ useEffect が存在する
    - 既存テストがリグレッションなしでパスする（App.test.tsx が存在する場合）
  </acceptance_criteria>
  <done>App.tsx が起動時に reorderStore.loadFromStorage() を呼び、plugin-store からカスタム順序を復元する。REORD-02 の起動時復元が完了。</done>
</task>

<task type="auto">
  <name>Task 2: Board.tsx handleDragEnd + filteredAndSortedView + IssueCard.tsx disabled 条件</name>
  <files>src/components/Board/Board.tsx, src/components/Board/Board.test.tsx, src/components/IssueCard/IssueCard.tsx, src/components/IssueCard/IssueCard.test.tsx</files>
  <read_first>
    - src/components/Board/Board.tsx (handleDragEnd の現在の実装、filteredAndSortedView パイプライン)
    - src/components/Board/Board.test.tsx (既存テストのモック構造)
    - src/components/IssueCard/IssueCard.tsx (useSortable の現在の disabled 条件)
    - src/components/IssueCard/IssueCard.test.tsx (既存テストのモック構造)
    - src/stores/reorderStore.ts (Plan 01 で作成した useReorderStore のインターフェース)
    - src/utils/reorderUtils.ts (Plan 01 で作成した applyCustomOrder のシグネチャ)
    - src/stores/sortStore.ts (sortField の値でソートモード検出)
  </read_first>
  <action>
**Board.tsx の変更:**

1. import 追加:
   ```typescript
   import { useReorderStore } from '../../stores/reorderStore';
   import { applyCustomOrder } from '../../utils/reorderUtils';
   ```

2. Board コンポーネント内に reorderStore セレクタを追加（既存の sortStore セレクタの直下）:
   ```typescript
   const orderMap = useReorderStore((s) => s.orderMap);
   const reorder = useReorderStore((s) => s.reorder);
   const updateOnCrossLaneMove = useReorderStore((s) => s.updateOnCrossLaneMove);
   ```

3. filteredAndSortedView の useMemo を拡張:
   - 依存配列に `orderMap` を追加
   - unassigned レーンの処理:
     ```typescript
     const unassignedSorted = applySortToIssues(unassignedFiltered, sortField, sortDirection);
     const unassignedOrdered = sortField === 'none'
       ? applyCustomOrder(unassignedSorted, orderMap['unassigned'] ?? [])
       : unassignedSorted;
     ```
   - milestone レーンの処理:
     ```typescript
     const sorted = applySortToIssues(filtered, sortField, sortDirection);
     const laneId = `milestone-${mwi.milestone.id}`;
     const ordered = sortField === 'none'
       ? applyCustomOrder(sorted, orderMap[laneId] ?? [])
       : sorted;
     ```
   - filteredIssues の値を `unassignedOrdered` / `ordered` に変更

4. handleDragEnd を拡張:
   ```typescript
   const handleDragEnd = (event: DragEndEvent) => {
     setActiveIssue(null);
     setOverLaneId(null);
     if (!data || !event.over) return;

     const fromLaneId = findLaneContaining(data, event.active.id as number);
     const toLaneId = resolveOverLaneId(data, event.over.id);

     if (!fromLaneId || !toLaneId) return;

     if (fromLaneId === toLaneId) {
       // レーン内並べ替え（REORD-01）
       // ソートモード中は何もしない（useSortable disabled で DnD 自体が発火しないが、安全のためガード）
       if (sortField !== 'none') return;
       const activeId = event.active.id as number;
       const overId = event.over.id as number;
       if (activeId !== overId) {
         // orderMap に laneId がない場合、現在の表示順（filteredAndSortedView）から初期 orderMap を構築
         const currentOrderMap = orderMap[fromLaneId];
         if (!currentOrderMap || currentOrderMap.length === 0) {
           // 初回並べ替え: 現在の表示順を orderMap に設定してから reorder
           const laneIssues = getLaneIssues(fromLaneId);
           const issueIds = laneIssues.map(i => i.id);
           useReorderStore.getState().setLaneOrder(fromLaneId, issueIds);
         }
         reorder(fromLaneId, activeId, overId);
       }
     } else {
       // レーン間移動（既存ロジック）
       moveIssue(event.active.id as number, fromLaneId, toLaneId);
       updateOnCrossLaneMove(event.active.id as number, fromLaneId, toLaneId);
     }
   };
   ```

5. getLaneIssues ヘルパー関数を Board コンポーネント内（handleDragEnd の前）に追加:
   ```typescript
   /** filteredAndSortedView から指定レーンの課題リストを取得する */
   const getLaneIssues = (laneId: string): BacklogIssue[] => {
     if (!filteredAndSortedView) return [];
     if (laneId === 'unassigned') return filteredAndSortedView.unassigned.filteredIssues;
     const ms = filteredAndSortedView.milestones.find(
       m => `milestone-${m.milestone.id}` === laneId
     );
     return ms?.filteredIssues ?? [];
   };
   ```

**IssueCard.tsx の変更:**

1. import 追加:
   ```typescript
   import { useSortStore } from '../../stores/sortStore';
   ```

2. IssueCard コンポーネント内、`isMultiMilestone` 計算の直下に追加:
   ```typescript
   const isSortActive = useSortStore((s) => s.field !== 'none');
   ```

3. useSortable の disabled 条件を変更:
   ```typescript
   const { attributes, listeners, setNodeRef, transform, transition, isDragging } = useSortable({
     id: issue.id,
     disabled: isMultiMilestone || isSortActive,
   });
   ```

4. className に isSortActive 時のスタイル追加:
   ```typescript
   className={`${styles.card} ${isDragging ? styles.cardDragging : ''} ${isMultiMilestone || isSortActive ? styles.cardDragDisabled : ''}`}
   ```

**Board.test.tsx の変更:**

- reorderStore のモック追加:
  ```typescript
  vi.mock('../../stores/reorderStore', () => ({
    useReorderStore: vi.fn((selector) => selector({
      orderMap: {},
      reorder: vi.fn(),
      updateOnCrossLaneMove: vi.fn(),
      setLaneOrder: vi.fn(),
      removeLaneOrder: vi.fn(),
      loadFromStorage: vi.fn(),
    })),
  }));
  ```
- reorderUtils のモック追加:
  ```typescript
  vi.mock('../../utils/reorderUtils', () => ({
    applyCustomOrder: vi.fn((issues) => issues),
  }));
  ```
- 新規テスト追加: `sortField === 'none' の場合 applyCustomOrder が呼ばれる`

**IssueCard.test.tsx の変更:**

- sortStore のモック追加（まだモックされていない場合）:
  ```typescript
  vi.mock('../../stores/sortStore', () => ({
    useSortStore: vi.fn((selector) => selector({ field: 'none', direction: 'asc' })),
  }));
  ```
- 新規テスト追加: `sortStore.field !== 'none' の場合 useSortable に disabled: true が渡される`
  </action>
  <verify>
    <automated>npx vitest run src/components/Board/Board.test.tsx src/components/IssueCard/IssueCard.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/Board/Board.tsx に `import { useReorderStore }` が存在する
    - src/components/Board/Board.tsx に `import { applyCustomOrder }` が存在する
    - src/components/Board/Board.tsx の handleDragEnd に `fromLaneId === toLaneId` 分岐が存在する
    - src/components/Board/Board.tsx の handleDragEnd に `reorder(fromLaneId, activeId, overId)` 呼び出しが存在する
    - src/components/Board/Board.tsx の handleDragEnd に `updateOnCrossLaneMove` 呼び出しが存在する
    - src/components/Board/Board.tsx の filteredAndSortedView に `applyCustomOrder` 呼び出しが存在する
    - src/components/Board/Board.tsx の filteredAndSortedView に `sortField === 'none'` 条件分岐が存在する
    - src/components/Board/Board.tsx の useMemo 依存配列に `orderMap` が含まれる
    - src/components/IssueCard/IssueCard.tsx に `useSortStore` の import が存在する
    - src/components/IssueCard/IssueCard.tsx に `isSortActive` 変数が存在する
    - src/components/IssueCard/IssueCard.tsx の useSortable に `disabled: isMultiMilestone || isSortActive` が存在する
    - `npx vitest run src/components/Board/Board.test.tsx src/components/IssueCard/IssueCard.test.tsx` が全件パスする
  </acceptance_criteria>
  <done>Board.tsx に同一レーン並べ替え + カスタム順序パイプライン + レーン間移動時の orderMap 更新が実装済み。IssueCard.tsx にソートモード時の disabled 条件が追加済み。REORD-01/02/03 + D-01~D-08 がすべてコードレベルで実装完了。全テストパス。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: レーン内並べ替え機能の E2E 動作確認</name>
  <files>N/A</files>
  <action>
レーン内カード並べ替え機能（REORD-01/02/03）の完全な実装を人間が確認する。カスタム順序の永続化、ソートモード排他制御、レーン間移動との連携を検証する。

`npm run tauri dev` でアプリを起動し、以下を順に確認:

1. **レーン内並べ替え（REORD-01）:**
   - ソートが「ソートなし」の状態で、任意のレーン内のカードをドラッグして別の位置にドロップ
   - カードが新しい位置に移動し、アニメーション付きで他のカードが押し出される
   - 複数回並べ替えても順序が正しく反映される

2. **永続化（REORD-02）:**
   - カードを並べ替えた後、アプリを終了して再起動
   - 並べ替えた順序が復元されていることを確認

3. **ソートモード排他（REORD-03, D-05）:**
   - ソートドロップダウンから「担当者」または「期限日」を選択
   - レーン内のカードをドラッグしようとしてもドラッグが開始されないことを確認
   - カーソルが grab ハンドルではなくなっていることを確認

4. **ソートモード中のレーン間DnD維持（D-06）:**
   - ソートが有効な状態で、カードを別のレーンにドラッグ&ドロップ
   - マイルストーン移動が正常に動作することを確認

5. **ソート解除後のカスタム順序復帰（D-01）:**
   - カードを並べ替え -> ソートを有効化（順序が変わる）-> ソートを「ソートなし」に戻す
   - 並べ替えた順序が復帰していることを確認

6. **レーン間移動後の順序処理（D-07, D-08）:**
   - カードをレーンAからレーンBにドラッグ移動
   - レーンBの末尾にカードが追加されていることを確認
   - レーンAから移動したカードが消えていることを確認

7. **フルスイートリグレッション:**
   ```bash
   npx vitest run
   ```
   全件パスを確認
  </action>
  <verify>"approved" と入力するか、問題点を記述してください</verify>
  <done>人間が REORD-01/02/03 + D-01~D-08 のすべての動作を確認し approved を返した。</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| plugin-store -> Board.tsx | orderMap のデータが applyCustomOrder に渡される（Plan 01 の T-08-01 でバリデーション済み） |
| DnD event -> handleDragEnd | event.active.id / event.over.id が number として使用される |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-03 | Tampering | Board.tsx handleDragEnd | accept | DnD イベントの active.id / over.id は dnd-kit 内部で生成されるもので、ユーザーが直接注入する攻撃ベクタはない。ローカルデスクトップアプリのため外部攻撃リスクなし |
| T-08-04 | Information Disclosure | IssueCard.tsx useSortStore | accept | sortStore.field はクライアントサイドの状態のみ。機密情報を含まない |
</threat_model>

<verification>
```bash
# 全テストが通ること
npx vitest run

# Board.tsx の統合確認
grep "useReorderStore" src/components/Board/Board.tsx
grep "applyCustomOrder" src/components/Board/Board.tsx
grep "fromLaneId === toLaneId" src/components/Board/Board.tsx
grep "updateOnCrossLaneMove" src/components/Board/Board.tsx

# IssueCard.tsx の統合確認
grep "isSortActive" src/components/IssueCard/IssueCard.tsx
grep "useSortStore" src/components/IssueCard/IssueCard.tsx

# App.tsx の統合確認
grep "useReorderStore" src/App.tsx
grep "loadReorderFromStorage" src/App.tsx
```
</verification>

<success_criteria>
- Board.tsx handleDragEnd が同一レーン並べ替え（REORD-01）を処理する
- Board.tsx filteredAndSortedView が sortField==='none' 時に applyCustomOrder を適用する（D-01）
- Board.tsx handleDragEnd がレーン間移動時に updateOnCrossLaneMove を呼ぶ（D-07, D-08）
- IssueCard.tsx useSortable が sortField!=='none' 時に disabled: true になる（REORD-03, D-05）
- App.tsx が起動時に reorderStore.loadFromStorage() を呼ぶ（REORD-02）
- `npx vitest run` フルスイートがリグレッションなしで全件パス
- 人間による E2E 動作確認が approved
</success_criteria>

<output>
After completion, create `.planning/phases/08-reorder/08-02-SUMMARY.md`
</output>
