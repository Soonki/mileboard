---
phase: 03-core-kanban-board
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/StatusBadge/StatusBadge.tsx
  - src/components/StatusBadge/StatusBadge.module.css
  - src/components/StatusBadge/StatusBadge.test.tsx
  - src/components/PriorityIndicator/PriorityIndicator.tsx
  - src/components/PriorityIndicator/PriorityIndicator.module.css
  - src/components/PriorityIndicator/PriorityIndicator.test.tsx
  - src/components/IssueCard/IssueCard.tsx
  - src/components/IssueCard/IssueCard.module.css
  - src/components/IssueCard/IssueCard.test.tsx
  - src/components/EmptyLane/EmptyLane.tsx
  - src/components/EmptyLane/EmptyLane.module.css
  - src/components/EmptyLane/EmptyLane.test.tsx
autonomous: true
requirements: [BOARD-03]

must_haves:
  truths:
    - "IssueCard renders issueKey, summary (truncated), status badge, assignee name, and priority arrows"
    - "StatusBadge displays Backlog status name in uniform gray style"
    - "PriorityIndicator maps priority id 2/3/4 to arrow counts and colors correctly"
    - "Missing assignee shows '---' placeholder, missing priority shows nothing"
    - "EmptyLane shows 'kousai nashi' text for milestones with zero issues"
  artifacts:
    - path: "src/components/StatusBadge/StatusBadge.tsx"
      provides: "Status badge with gray background"
      exports: ["StatusBadge"]
    - path: "src/components/PriorityIndicator/PriorityIndicator.tsx"
      provides: "Priority arrow indicator"
      exports: ["PriorityIndicator"]
    - path: "src/components/IssueCard/IssueCard.tsx"
      provides: "3-line issue card layout"
      exports: ["IssueCard"]
    - path: "src/components/EmptyLane/EmptyLane.tsx"
      provides: "Empty lane placeholder"
      exports: ["EmptyLane"]
  key_links:
    - from: "src/components/IssueCard/IssueCard.tsx"
      to: "src/components/StatusBadge/StatusBadge.tsx"
      via: "import { StatusBadge }"
      pattern: "import.*StatusBadge"
    - from: "src/components/IssueCard/IssueCard.tsx"
      to: "src/components/PriorityIndicator/PriorityIndicator.tsx"
      via: "import { PriorityIndicator }"
      pattern: "import.*PriorityIndicator"
---

<objective>
Create the leaf-level presentational components: StatusBadge, PriorityIndicator, IssueCard, and EmptyLane. These are pure display components with no store dependency.

Purpose: These components form the card-level rendering layer of the Kanban board. IssueCard is the core visual unit displaying each Backlog issue. StatusBadge and PriorityIndicator are sub-components extracted for reuse and testability.

Output: 4 components with CSS Modules and unit tests, all matching UI-SPEC pixel specifications.
</objective>

<execution_context>
@C:\Users\sungi\.claude\get-shit-done\workflows\execute-plan.md
@C:\Users\sungi\.claude\get-shit-done\templates\summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-core-kanban-board/03-CONTEXT.md
@.planning/phases/03-core-kanban-board/03-RESEARCH.md
@.planning/phases/03-core-kanban-board/03-UI-SPEC.md
@.planning/phases/03-core-kanban-board/03-VALIDATION.md

<interfaces>
<!-- Key types the executor needs. -->

From src/types/backlog.ts:
```typescript
export interface BacklogStatus {
  id: number;
  projectId: number | null;
  name: string;
  color: string;
  displayOrder: number;
}

export interface BacklogPriority {
  id: number;
  name: string;
}

export interface BacklogUser {
  id: number;
  userId: string;
  name: string;
  roleType: number;
  mailAddress: string;
}

export interface BacklogIssue {
  id: number;
  projectId: number;
  issueKey: string;
  keyId: number;
  summary: string;
  description: string | null;
  status: BacklogStatus;
  priority: BacklogPriority | null;
  assignee: BacklogUser | null;
  milestone: BacklogMilestone[];
  category: BacklogCategory[];
  startDate: string | null;
  dueDate: string | null;
  created: string;
  updated: string;
}
```

From src/global.css (relevant tokens):
```css
--space-xs: 4px;
--space-sm: 8px;
--font-size-sm: 12px;
--font-size-body: 14px;
--font-weight-regular: 400;
--font-weight-semibold: 600;
--line-height-body: 1.5;
--line-height-label: 1.4;
--color-bg: #F8F9FA;
--color-surface: #FFFFFF;
--color-error: #DC2626;
--color-success: #16A34A;
--color-warning: #F59E0B;    /* added in Plan 01 */
--color-disabled: #9CA3AF;
--color-border: #D1D5DB;
--color-text-primary: #111827;
--color-text-secondary: #6B7280;
--radius-input: 6px;
--radius-badge: 4px;          /* added in Plan 01 */
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create StatusBadge, PriorityIndicator, IssueCard, EmptyLane components with CSS Modules</name>
  <files>
    src/components/StatusBadge/StatusBadge.tsx,
    src/components/StatusBadge/StatusBadge.module.css,
    src/components/PriorityIndicator/PriorityIndicator.tsx,
    src/components/PriorityIndicator/PriorityIndicator.module.css,
    src/components/IssueCard/IssueCard.tsx,
    src/components/IssueCard/IssueCard.module.css,
    src/components/EmptyLane/EmptyLane.tsx,
    src/components/EmptyLane/EmptyLane.module.css
  </files>
  <read_first>
    - src/types/backlog.ts (BacklogIssue, BacklogStatus, BacklogPriority, BacklogUser types)
    - src/global.css (design tokens including Phase 3 additions from Plan 01)
    - .planning/phases/03-core-kanban-board/03-UI-SPEC.md (pixel-level component specs)
    - src/components/SettingsForm/SettingsForm.tsx (existing component pattern reference)
    - src/components/SettingsForm/SettingsForm.module.css (existing CSS Module pattern)
  </read_first>
  <action>
    Create 4 component directories following the established `src/components/ComponentName/` pattern.

    **1. StatusBadge (per D-06, UI-SPEC StatusBadge section):**

    `StatusBadge.tsx`:
    ```typescript
    interface StatusBadgeProps {
      name: string;
    }
    ```
    - Render a `<span>` with `className={styles.badge}` and `aria-label={name}`
    - Display `{name}` text content
    - Export named: `export function StatusBadge`

    `StatusBadge.module.css`:
    ```css
    .badge {
      font-size: var(--font-size-sm);        /* 12px */
      font-weight: var(--font-weight-regular); /* 400 */
      padding: var(--space-xs) var(--space-sm); /* 4px 8px */
      background: var(--color-bg);             /* #F8F9FA */
      border: 1px solid var(--color-border);   /* #D1D5DB */
      border-radius: var(--radius-badge);      /* 4px */
      color: var(--color-text-secondary);      /* #6B7280 */
      white-space: nowrap;
      max-width: 80px;
      overflow: hidden;
      text-overflow: ellipsis;
      display: inline-block;
      line-height: var(--line-height-label);   /* 1.4 */
    }
    ```

    **2. PriorityIndicator (per D-05, UI-SPEC PriorityIndicator section):**

    `PriorityIndicator.tsx`:
    ```typescript
    import type { BacklogPriority } from '../../types/backlog';

    interface PriorityIndicatorProps {
      priority: BacklogPriority | null;
    }
    ```
    - If `priority` is null, return `null` (render nothing)
    - Map priority.id to display:
      - `id === 2` (high): text=`'▲▲▲'`, className=`styles.high`
      - `id === 3` (medium): text=`'▲▲'`, className=`styles.medium`
      - `id === 4` (low): text=`'▲'`, className=`styles.low`
      - default: return `null`
    - Render: `<span className={classForId} aria-label={priority.name}>{arrowText}</span>`
    - Export named: `export function PriorityIndicator`

    `PriorityIndicator.module.css`:
    ```css
    .high, .medium, .low {
      font-size: var(--font-size-sm);  /* 12px */
      letter-spacing: -1px;
      line-height: var(--line-height-label); /* 1.4 */
    }
    .high { color: var(--color-error); }     /* #DC2626 */
    .medium { color: var(--color-warning); } /* #F59E0B */
    .low { color: var(--color-success); }    /* #16A34A */
    ```

    **3. IssueCard (per D-04, UI-SPEC IssueCard section):**

    `IssueCard.tsx`:
    ```typescript
    import type { BacklogIssue } from '../../types/backlog';
    import { StatusBadge } from '../StatusBadge/StatusBadge';
    import { PriorityIndicator } from '../PriorityIndicator/PriorityIndicator';
    ```
    - Props: `interface IssueCardProps { issue: BacklogIssue; }`
    - 3-line layout in a `<div className={styles.card}>`:
      - Line 1 (`styles.line1`): `<span className={styles.issueKey}>{issue.issueKey}</span>` + `<StatusBadge name={issue.status.name} />`
      - Line 2 (`styles.summary`): `{issue.summary}` with CSS ellipsis truncation
      - Line 3 (`styles.line3`): `<span className={styles.assignee}>{issue.assignee?.name ?? '---'}</span>` + `<PriorityIndicator priority={issue.priority} />`
    - When assignee is null, show `'---'` with className `styles.noAssignee` (using --color-disabled)
    - Export named: `export function IssueCard`

    `IssueCard.module.css`:
    ```css
    .card {
      padding: var(--space-sm);               /* 8px */
      margin: 0 var(--space-sm) var(--space-sm); /* 0 8px 8px */
      background: var(--color-surface);        /* #FFFFFF */
      border: 1px solid var(--color-border);   /* #D1D5DB */
      border-radius: var(--radius-input);      /* 6px */
      cursor: default;
    }
    .card:first-child { margin-top: var(--space-sm); } /* 8px */
    .line1 {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: var(--space-xs);          /* 4px */
    }
    .issueKey {
      font-size: var(--font-size-sm);          /* 12px */
      font-weight: var(--font-weight-semibold); /* 600 */
      color: var(--color-text-primary);
    }
    .summary {
      font-size: var(--font-size-body);        /* 14px */
      font-weight: var(--font-weight-regular); /* 400 */
      color: var(--color-text-primary);
      white-space: nowrap;
      overflow: hidden;
      text-overflow: ellipsis;
      margin-bottom: var(--space-xs);          /* 4px */
    }
    .line3 {
      display: flex;
      justify-content: space-between;
      align-items: center;
    }
    .assignee {
      font-size: var(--font-size-sm);          /* 12px */
      font-weight: var(--font-weight-regular); /* 400 */
      color: var(--color-text-secondary);
    }
    .noAssignee {
      font-size: var(--font-size-sm);
      font-weight: var(--font-weight-regular);
      color: var(--color-disabled);            /* #9CA3AF */
    }
    ```

    **4. EmptyLane:**

    `EmptyLane.tsx`:
    - No props (or empty interface)
    - Render: `<div className={styles.empty}>課題なし</div>`
    - Export named: `export function EmptyLane`

    `EmptyLane.module.css`:
    ```css
    .empty {
      display: flex;
      flex-direction: column;
      align-items: center;
      justify-content: center;
      padding: var(--space-2xl) 0;             /* 48px vertical */
      font-size: var(--font-size-sm);          /* 12px */
      color: var(--color-text-secondary);      /* #6B7280 */
    }
    ```
  </action>
  <verify>
    <automated>npx vitest run src/components/StatusBadge src/components/PriorityIndicator src/components/IssueCard src/components/EmptyLane -x</automated>
  </verify>
  <acceptance_criteria>
    - src/components/StatusBadge/StatusBadge.tsx contains `export function StatusBadge`
    - src/components/StatusBadge/StatusBadge.tsx contains `aria-label={name}`
    - src/components/StatusBadge/StatusBadge.module.css contains `var(--color-bg)` for background
    - src/components/StatusBadge/StatusBadge.module.css contains `var(--radius-badge)` for border-radius
    - src/components/PriorityIndicator/PriorityIndicator.tsx contains `export function PriorityIndicator`
    - src/components/PriorityIndicator/PriorityIndicator.tsx maps id 2 to `▲▲▲`, id 3 to `▲▲`, id 4 to `▲`
    - src/components/PriorityIndicator/PriorityIndicator.module.css contains `var(--color-error)` for .high
    - src/components/PriorityIndicator/PriorityIndicator.module.css contains `var(--color-warning)` for .medium
    - src/components/PriorityIndicator/PriorityIndicator.module.css contains `var(--color-success)` for .low
    - src/components/IssueCard/IssueCard.tsx contains `export function IssueCard`
    - src/components/IssueCard/IssueCard.tsx contains `issue.assignee?.name ?? '---'`
    - src/components/IssueCard/IssueCard.tsx imports StatusBadge and PriorityIndicator
    - src/components/IssueCard/IssueCard.module.css contains `text-overflow: ellipsis` for summary
    - src/components/EmptyLane/EmptyLane.tsx contains `課題なし`
    - No files contain hardcoded hex color values (all use CSS variables)
    - No files contain `console.log`
  </acceptance_criteria>
  <done>4 presentational components created: StatusBadge (gray badge), PriorityIndicator (arrow mapping with colors), IssueCard (3-line compact layout), EmptyLane (placeholder text). All use CSS Modules with design token variables.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create unit tests for StatusBadge, PriorityIndicator, IssueCard, EmptyLane</name>
  <files>
    src/components/StatusBadge/StatusBadge.test.tsx,
    src/components/PriorityIndicator/PriorityIndicator.test.tsx,
    src/components/IssueCard/IssueCard.test.tsx,
    src/components/EmptyLane/EmptyLane.test.tsx
  </files>
  <read_first>
    - src/components/StatusBadge/StatusBadge.tsx (component to test)
    - src/components/PriorityIndicator/PriorityIndicator.tsx (component to test)
    - src/components/IssueCard/IssueCard.tsx (component to test)
    - src/components/EmptyLane/EmptyLane.tsx (component to test)
    - src/types/backlog.ts (type definitions for test fixtures)
    - tests/setup.ts (global test setup)
  </read_first>
  <behavior>
    StatusBadge tests:
    - Renders status name text
    - Has aria-label matching status name
    - Renders with badge CSS class

    PriorityIndicator tests:
    - Renders nothing when priority is null
    - Renders 3 arrows for high priority (id=2)
    - Renders 2 arrows for medium priority (id=3)
    - Renders 1 arrow for low priority (id=4)
    - Renders nothing for unknown priority id (e.g., id=99)
    - Has aria-label with priority name

    IssueCard tests:
    - Renders issue key
    - Renders summary text
    - Renders status badge with status name
    - Renders assignee name
    - Renders '---' when assignee is null
    - Renders priority indicator
    - Renders nothing for priority when priority is null

    EmptyLane tests:
    - Renders empty lane text
  </behavior>
  <action>
    Create test files using RTL (`@testing-library/react`) and Vitest. Use `render`, `screen` from RTL. Follow existing test patterns from Phase 1/2.

    **Test fixture helper** (create in each test file or inline):
    ```typescript
    function createMockIssue(overrides?: Partial<BacklogIssue>): BacklogIssue {
      return {
        id: 1, projectId: 1, issueKey: 'TEST-1', keyId: 1,
        summary: 'テスト課題のサマリー',
        description: null,
        status: { id: 1, projectId: 1, name: '未対応', color: '#ed8077', displayOrder: 1000 },
        priority: { id: 3, name: '中' },
        assignee: { id: 1, userId: 'user1', name: '山田太郎', roleType: 1, mailAddress: 'test@test.com' },
        milestone: [], category: [],
        startDate: null, dueDate: null,
        created: '2025-04-01T00:00:00Z', updated: '2025-04-01T00:00:00Z',
        ...overrides,
      };
    }
    ```

    **StatusBadge.test.tsx (3 tests):**
    1. `'renders status name text'` -- `render(<StatusBadge name="未対応" />)`, `expect(screen.getByText('未対応')).toBeInTheDocument()`
    2. `'has aria-label matching status name'` -- `expect(screen.getByLabelText('未対応')).toBeInTheDocument()`
    3. `'renders long status name with ellipsis class'` -- render with long name, verify element exists (CSS truncation not testable in jsdom)

    **PriorityIndicator.test.tsx (6 tests):**
    1. `'renders nothing when priority is null'` -- `const { container } = render(...)`, `expect(container.firstChild).toBeNull()`
    2. `'renders 3 arrows for high priority (id=2)'` -- `expect(screen.getByText('▲▲▲')).toBeInTheDocument()`
    3. `'renders 2 arrows for medium priority (id=3)'` -- `expect(screen.getByText('▲▲')).toBeInTheDocument()`
    4. `'renders 1 arrow for low priority (id=4)'` -- `expect(screen.getByText('▲')).toBeInTheDocument()`
    5. `'renders nothing for unknown priority id'` -- priority `{ id: 99, name: 'unknown' }`, expect null
    6. `'has aria-label with priority name'` -- `expect(screen.getByLabelText('高')).toBeInTheDocument()`

    **IssueCard.test.tsx (7 tests):**
    1. `'renders issue key'` -- `expect(screen.getByText('TEST-1')).toBeInTheDocument()`
    2. `'renders summary text'` -- `expect(screen.getByText('テスト課題のサマリー')).toBeInTheDocument()`
    3. `'renders status badge'` -- `expect(screen.getByText('未対応')).toBeInTheDocument()`
    4. `'renders assignee name'` -- `expect(screen.getByText('山田太郎')).toBeInTheDocument()`
    5. `'renders --- when assignee is null'` -- issue with `assignee: null`, `expect(screen.getByText('---')).toBeInTheDocument()`
    6. `'renders priority indicator'` -- `expect(screen.getByText('▲▲')).toBeInTheDocument()` (medium, id=3)
    7. `'renders no priority indicator when priority is null'` -- issue with `priority: null`, verify `▲` does not appear

    **EmptyLane.test.tsx (1 test):**
    1. `'renders empty lane text'` -- `expect(screen.getByText('課題なし')).toBeInTheDocument()`
  </action>
  <verify>
    <automated>npx vitest run src/components/StatusBadge src/components/PriorityIndicator src/components/IssueCard src/components/EmptyLane -x</automated>
  </verify>
  <acceptance_criteria>
    - src/components/StatusBadge/StatusBadge.test.tsx contains 3+ test cases
    - src/components/PriorityIndicator/PriorityIndicator.test.tsx contains 6+ test cases
    - src/components/IssueCard/IssueCard.test.tsx contains 7+ test cases
    - src/components/EmptyLane/EmptyLane.test.tsx contains 1+ test cases
    - All tests pass with `npx vitest run src/components/StatusBadge src/components/PriorityIndicator src/components/IssueCard src/components/EmptyLane -x`
    - `npx vitest run` passes with no regressions
  </acceptance_criteria>
  <done>17 unit tests pass across 4 component test files: StatusBadge (3), PriorityIndicator (6), IssueCard (7), EmptyLane (1). All covering display correctness, null handling, and accessibility labels.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| BacklogIssue data -> IssueCard | External API data rendered in DOM. Potential XSS via issue summary or status name. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Information Disclosure | IssueCard | accept | Cards display Backlog issue data (key, summary, assignee). This is the app's purpose. No sensitive data beyond what Backlog API returns. Desktop app, single user. |
| T-03-05 | Tampering (XSS) | IssueCard/StatusBadge | mitigate | React's JSX auto-escapes all string interpolation (`{issue.summary}`, `{issue.status.name}`). No `dangerouslySetInnerHTML` used. All text rendered via React text nodes, not innerHTML. |
| T-03-06 | Tampering (XSS) | PriorityIndicator | accept | Priority arrows are hardcoded Unicode strings (`▲`), not user-supplied. Priority name used only in aria-label (auto-escaped by React). |
</threat_model>

<verification>
1. `npx vitest run src/components/StatusBadge src/components/PriorityIndicator src/components/IssueCard src/components/EmptyLane -x` -- all 17 tests pass
2. `npx vitest run` -- full suite passes, no regressions
3. Verify no hardcoded colors: `grep -r '#[0-9a-fA-F]\{6\}' src/components/StatusBadge/ src/components/PriorityIndicator/ src/components/IssueCard/ src/components/EmptyLane/` returns no matches in CSS files
</verification>

<success_criteria>
- StatusBadge renders status name in gray badge with proper aria-label
- PriorityIndicator correctly maps id 2->3 arrows (red), id 3->2 arrows (orange), id 4->1 arrow (green), null->nothing
- IssueCard shows 3-line layout with key, summary (ellipsis), assignee, priority
- IssueCard handles null assignee ('---') and null priority (hidden)
- EmptyLane shows placeholder text
- 17 tests pass, no regressions
</success_criteria>

<output>
After completion, create `.planning/phases/03-core-kanban-board/03-02-SUMMARY.md`
</output>
