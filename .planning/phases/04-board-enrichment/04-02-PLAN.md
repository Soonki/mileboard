---
phase: 04-board-enrichment
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - src/components/LaneHeader/LaneHeader.tsx
  - src/components/LaneHeader/LaneHeader.module.css
  - src/components/LaneHeader/LaneHeader.test.tsx
  - src/components/Lane/Lane.tsx
  - src/components/Lane/Lane.test.tsx
autonomous: false
requirements:
  - BOARD-05
  - BOARD-06

must_haves:
  truths:
    - "Lane header displays issue count in parentheses next to milestone name"
    - "Lane header has toggle button that expands/collapses member breakdown"
    - "Member breakdown shows member-by-member issue counts sorted descending"
    - "Unassigned issues appear last in breakdown with '未割当' label"
    - "Toggle button shows triangle icon and is only visible when breakdown has items"
    - "Expand/collapse has smooth CSS transition animation"
  artifacts:
    - path: "src/components/LaneHeader/LaneHeader.tsx"
      provides: "Lane header with issue count, toggle, and member breakdown"
      exports: ["LaneHeader"]
    - path: "src/components/Lane/Lane.tsx"
      provides: "Lane computing and passing stats to LaneHeader"
      exports: ["Lane"]
  key_links:
    - from: "src/components/Lane/Lane.tsx"
      to: "src/utils/memberBreakdown.ts"
      via: "computeMemberBreakdown(issues) import"
      pattern: "computeMemberBreakdown"
    - from: "src/components/Lane/Lane.tsx"
      to: "src/components/LaneHeader/LaneHeader.tsx"
      via: "issueCount and memberBreakdown props"
      pattern: "issueCount=.*memberBreakdown="
    - from: "src/components/LaneHeader/LaneHeader.tsx"
      to: "src/components/MemberBreakdown/MemberBreakdown.tsx"
      via: "MemberBreakdown component render on expand"
      pattern: "MemberBreakdown"
---

<objective>
LaneHeaderにissue count表示とメンバー別内訳のトグル展開UIを実装し、LaneからLaneHeaderへのデータ受け渡しを完成させる。最後にビジュアル検証チェックポイントでPhase 4全体を確認する。

Purpose: BOARD-05(レーン課題数)とBOARD-06(メンバー別内訳)を実現し、Phase 4の全要件を完成させる。
Output: LaneHeader拡張、Lane wiring、ビジュアル検証完了
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-board-enrichment/04-CONTEXT.md
@.planning/phases/04-board-enrichment/04-RESEARCH.md
@.planning/phases/04-board-enrichment/04-UI-SPEC.md
@.planning/phases/04-board-enrichment/04-VALIDATION.md
@.planning/phases/04-board-enrichment/04-01-SUMMARY.md

<interfaces>
<!-- Interfaces created by Plan 01 that this plan depends on -->

From src/utils/memberBreakdown.ts (created in Plan 01):
```typescript
export interface MemberCount {
  name: string;
  count: number;
}

export function computeMemberBreakdown(issues: BacklogIssue[]): MemberCount[];
```

From src/components/MemberBreakdown/MemberBreakdown.tsx (created in Plan 01):
```typescript
import type { MemberCount } from '../../utils/memberBreakdown';

interface MemberBreakdownProps {
  members: MemberCount[];
}

export function MemberBreakdown({ members }: MemberBreakdownProps) { ... }
// Returns null when members.length === 0
// Renders <ul role="list" aria-label="メンバー別課題数">
```

From src/components/LaneHeader/LaneHeader.tsx (current, to be modified):
```typescript
interface LaneHeaderProps {
  name: string;
  startDate: string | null;
  releaseDueDate: string | null;
}

export function LaneHeader({ name, startDate, releaseDueDate }: LaneHeaderProps) {
  // renders: <div className={styles.header}>
  //            <div className={styles.name}>{name}</div>
  //            {dateRange && <div className={styles.dateRange}>{dateRange}</div>}
  //          </div>
}
```

From src/components/Lane/Lane.tsx (current, to be modified):
```typescript
interface LaneProps {
  name: string;
  startDate: string | null;
  releaseDueDate: string | null;
  issues: BacklogIssue[];
}

export function Lane({ name, startDate, releaseDueDate, issues }: LaneProps) {
  // renders: <LaneHeader name={name} startDate={startDate} releaseDueDate={releaseDueDate} />
}
```

From src/types/backlog.ts:
```typescript
export interface BacklogIssue {
  // ... (see Plan 01 interfaces)
  assignee: BacklogUser | null;
}
```

From src/global.css (relevant tokens):
```css
--transition-normal: 200ms ease-out;
--transition-fast: 150ms ease-out;
--space-xs: 4px;
--space-sm: 8px;
--space-md: 16px;
--color-text-secondary: #6B7280;
--color-text-primary: #111827;
--color-surface: #FFFFFF;
--color-border: #D1D5DB;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: LaneHeader拡張 + Lane wiring</name>
  <files>
    src/components/LaneHeader/LaneHeader.tsx
    src/components/LaneHeader/LaneHeader.module.css
    src/components/LaneHeader/LaneHeader.test.tsx
    src/components/Lane/Lane.tsx
    src/components/Lane/Lane.test.tsx
  </files>
  <read_first>
    src/components/LaneHeader/LaneHeader.tsx
    src/components/LaneHeader/LaneHeader.module.css
    src/components/Lane/Lane.tsx
    src/components/Lane/Lane.test.tsx
    src/components/MemberBreakdown/MemberBreakdown.tsx
    src/utils/memberBreakdown.ts
    src/types/backlog.ts
  </read_first>
  <behavior>
    LaneHeader:
    - LaneHeader displays issue count in parentheses after name: "Sprint 2504 (6)" (per D-08)
    - LaneHeader shows toggle button (down triangle) when memberBreakdown has items
    - LaneHeader does NOT show toggle button when memberBreakdown is empty
    - Clicking toggle button expands member breakdown panel
    - Clicking toggle button again collapses member breakdown panel
    - Expanded state shows MemberBreakdown component with members prop
    - Toggle button has aria-expanded attribute matching state
    - Toggle button has aria-label "内訳を開く" when collapsed and "内訳を閉じる" when expanded
    - Date range display is preserved after changes

    Lane:
    - Lane computes issueCount from issues.length and passes to LaneHeader
    - Lane computes memberBreakdown from issues and passes to LaneHeader
    - Lane passes computed data correctly for empty issues array
  </behavior>
  <action>
    1. **LaneHeader.test.tsx**: 新規作成。テストファーストで以下のケースを実装:
       ```typescript
       import { describe, it, expect } from 'vitest';
       import { render, screen } from '@testing-library/react';
       import userEvent from '@testing-library/user-event';
       import '@testing-library/jest-dom';
       import { LaneHeader } from './LaneHeader';
       ```
       テストケース:
       - `it('displays name with issue count in parentheses')`: render with name="Sprint 2504", issueCount=6 → getByText で "Sprint 2504 (6)" が含まれるテキストノードを確認
       - `it('shows toggle button when memberBreakdown has items')`: render with memberBreakdown=[{name:'Alice', count:3}] → ボタン要素が存在
       - `it('does not show toggle button when memberBreakdown is empty')`: render with memberBreakdown=[] → ボタンが存在しない
       - `it('expands breakdown on toggle click')`: render → click toggle → getByRole('list', { name: 'メンバー別課題数' }) が存在
       - `it('collapses breakdown on second toggle click')`: render → click → click → queryByRole('list') が null
       - `it('toggle has aria-expanded=false initially')`: ボタンの aria-expanded="false"
       - `it('toggle has aria-expanded=true after click')`: click → aria-expanded="true"
       - `it('toggle aria-label changes on expand')`: collapsed: "内訳を開く", expanded: "内訳を閉じる"
       - `it('preserves date range display')`: render with startDate="2025-04-01", releaseDueDate="2025-04-30" → "4/1~4/30" 表示

    2. **LaneHeader.tsx**: 大幅拡張(per D-03, D-04, D-08):
       - 先頭にimport追加:
         ```typescript
         import { useState } from 'react';
         import type { MemberCount } from '../../utils/memberBreakdown';
         import { MemberBreakdown } from '../MemberBreakdown/MemberBreakdown';
         ```
       - interface LaneHeaderProps を拡張:
         ```typescript
         interface LaneHeaderProps {
           name: string;
           startDate: string | null;
           releaseDueDate: string | null;
           issueCount: number;
           memberBreakdown: MemberCount[];
         }
         ```
       - formatDateRange 関数はそのまま維持
       - コンポーネント本体を書き換え:
         ```typescript
         export function LaneHeader({
           name, startDate, releaseDueDate, issueCount, memberBreakdown,
         }: LaneHeaderProps) {
           const [isExpanded, setIsExpanded] = useState(false);
           const dateRange = formatDateRange(startDate, releaseDueDate);

           return (
             <div className={styles.header}>
               <div className={styles.titleRow}>
                 <div className={styles.name}>
                   {name} ({issueCount})
                 </div>
                 {memberBreakdown.length > 0 && (
                   <button
                     className={styles.toggleButton}
                     onClick={() => setIsExpanded((prev) => !prev)}
                     aria-expanded={isExpanded}
                     aria-label={isExpanded ? '内訳を閉じる' : '内訳を開く'}
                   >
                     {isExpanded ? '\u25B2' : '\u25BC'}
                   </button>
                 )}
               </div>
               {dateRange !== null && (
                 <div className={styles.dateRange}>{dateRange}</div>
               )}
               <div
                 className={`${styles.breakdownContainer} ${isExpanded ? styles.expanded : ''}`}
               >
                 {isExpanded && <MemberBreakdown members={memberBreakdown} />}
               </div>
             </div>
           );
         }
         ```

    3. **LaneHeader.module.css**: レイアウト拡張:
       - `.header` は既存のまま維持(padding, border-bottom, sticky, bg, z-index)
       - `.titleRow` 追加:
         ```css
         .titleRow {
           display: flex;
           justify-content: space-between;
           align-items: center;
         }
         ```
       - `.name` は既存のまま維持(font-size, font-weight, color, line-height)
       - `.toggleButton` 追加:
         ```css
         .toggleButton {
           background: none;
           border: none;
           cursor: pointer;
           padding: var(--space-xs);
           font-size: var(--font-size-sm);
           color: var(--color-text-secondary);
           line-height: 1;
           transition: color var(--transition-fast);
         }

         .toggleButton:hover {
           color: var(--color-text-primary);
         }
         ```
       - `.dateRange` は既存のまま維持
       - `.breakdownContainer` 追加:
         ```css
         .breakdownContainer {
           overflow: hidden;
           max-height: 0;
           opacity: 0;
           transition: max-height var(--transition-normal), opacity var(--transition-normal);
         }

         .expanded {
           max-height: 300px;
           opacity: 1;
         }
         ```

    4. **Lane.tsx**: LaneHeaderへのデータ受け渡しを追加:
       - import追加:
         ```typescript
         import { computeMemberBreakdown } from '../../utils/memberBreakdown';
         ```
       - コンポーネント内でissueCountとmemberBreakdownを計算:
         ```typescript
         const issueCount = issues.length;
         const memberBreakdown = computeMemberBreakdown(issues);
         ```
       - LaneHeaderのpropsに追加:
         ```tsx
         <LaneHeader
           name={name}
           startDate={startDate}
           releaseDueDate={releaseDueDate}
           issueCount={issueCount}
           memberBreakdown={memberBreakdown}
         />
         ```

    5. **Lane.test.tsx**: 既存テストの更新 + 新テスト追加:
       - 既存テストは LaneHeader の新しいprops(issueCount, memberBreakdown)が Lane から自動的に渡されるため、テスト側の変更は不要(Lane は issues.length を計算して渡す)。ただし、テキスト検索が変わる可能性:
         - `'Sprint 2504'` → `'Sprint 2504 (0)'` になるため、既存の `getByText('Sprint 2504')` が失敗する可能性。`getByText(/Sprint 2504/)` に変更するか、完全一致テキストを更新
       - 新テスト追加:
         1. `it('displays issue count in lane header')`: issues=[2件] → "Sprint 2504 (2)" がテキストに含まれる
         2. `it('shows toggle button when issues have assignees')`: issues=[assignee付き2件] → トグルボタン存在
         3. `it('does not show toggle button when issues array is empty')`: issues=[] → トグルボタンなし
  </action>
  <verify>
    <automated>npx vitest run src/components/LaneHeader/LaneHeader.test.tsx src/components/Lane/Lane.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - src/components/LaneHeader/LaneHeader.tsx contains `issueCount: number`
    - src/components/LaneHeader/LaneHeader.tsx contains `memberBreakdown: MemberCount[]`
    - src/components/LaneHeader/LaneHeader.tsx contains `useState(false)`
    - src/components/LaneHeader/LaneHeader.tsx contains `{name} ({issueCount})`
    - src/components/LaneHeader/LaneHeader.tsx contains `aria-expanded={isExpanded}`
    - src/components/LaneHeader/LaneHeader.tsx contains `内訳を閉じる`
    - src/components/LaneHeader/LaneHeader.tsx contains `内訳を開く`
    - src/components/LaneHeader/LaneHeader.tsx contains `\u25B2`
    - src/components/LaneHeader/LaneHeader.tsx contains `\u25BC`
    - src/components/LaneHeader/LaneHeader.tsx contains `MemberBreakdown`
    - src/components/LaneHeader/LaneHeader.module.css contains `.titleRow`
    - src/components/LaneHeader/LaneHeader.module.css contains `.toggleButton`
    - src/components/LaneHeader/LaneHeader.module.css contains `.breakdownContainer`
    - src/components/LaneHeader/LaneHeader.module.css contains `.expanded`
    - src/components/Lane/Lane.tsx contains `computeMemberBreakdown`
    - src/components/Lane/Lane.tsx contains `issueCount={issueCount}`
    - src/components/Lane/Lane.tsx contains `memberBreakdown={memberBreakdown}`
    - npx vitest run src/components/LaneHeader src/components/Lane exits 0
  </acceptance_criteria>
  <done>
    LaneHeader: 課題数括弧表示、トグルボタン、MemberBreakdown展開/折りたたみ、全9テストgreen。
    Lane: computeMemberBreakdownでデータ計算、LaneHeaderに渡す。既存+新規3テストgreen。全既存テスト回帰なし。
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Phase 4 ビジュアル検証</name>
  <files>N/A</files>
  <action>
    Phase 4 Board Enrichment の全機能をビジュアル確認する。`npm run tauri dev` でアプリを起動し、
    接続設定が完了している状態でボードを表示して以下の16項目を検証する。
    自動テストではカバーできないCSSアニメーション、視覚的フィードバック、ブラウザ連携を対象とする。
  </action>
  <what-built>
    Phase 4 Board Enrichment の全機能:
    1. StatusBadge -- Backlogステータスカラーがバッジ背景に適用、テキスト色自動コントラスト(BOARD-04)
    2. LaneHeader -- マイルストーン名の横に課題数括弧表示(BOARD-05)
    3. LaneHeader -- トグルボタンでメンバー別内訳の展開/折りたたみ(BOARD-06)
    4. IssueCard -- クリックでBacklog課題ページをブラウザで開く + hover/rippleフィードバック(UX-03)
  </what-built>
  <how-to-verify>
    1. `npm run tauri dev` でアプリ起動
    2. 接続設定が完了している状態でボードを表示

    **BOARD-04 (ステータスカラー):**
    3. 課題カードのステータスバッジの背景色がBacklogのステータス色になっていることを確認
    4. バッジのテキストが背景に対して読みやすい色(白 or 黒)になっていることを確認
    5. 複数の異なるステータスのカードで色が区別できることを確認

    **BOARD-05 (課題数):**
    6. 各レーンヘッダにマイルストーン名の後ろに括弧で課題数が表示されていることを確認 (例: "Sprint 2504 (6)")

    **BOARD-06 (メンバー内訳):**
    7. 課題が存在するレーンのヘッダ右端にトグルボタン(三角アイコン)が表示されていることを確認
    8. トグルボタンをクリックしてメンバー別の課題数内訳が展開されることを確認
    9. メンバーが課題数の多い順に並んでいることを確認
    10. 未割当の課題がある場合、"未割当" が最後に表示されていることを確認
    11. もう一度クリックして折りたたまれることを確認
    12. 展開/折りたたみがスムーズにアニメーションすることを確認

    **UX-03 (カードクリック):**
    13. 課題カードにマウスホバーして背景色が変わることを確認
    14. カーソルがpointerになっていることを確認
    15. カードをクリックしてデフォルトブラウザでBacklog課題ページが開くことを確認
    16. クリック時にrippleアニメーションが表示されることを確認
  </how-to-verify>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>
    ビジュアル検証で16項目全て確認完了。Phase 4の全要件(BOARD-04, BOARD-05, BOARD-06, UX-03)が動作確認済み。
  </done>
  <resume-signal>"approved" と入力するか、問題点を記述してください</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| issues array -> LaneHeader stats | boardStoreのissues配列がLaneHeader統計に使用される(信頼済みデータ) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Information Disclosure | MemberBreakdown member names | accept | メンバー名はBacklog APIから取得済みの信頼データ。ローカルアプリ内表示のみ。外部送信なし |
</threat_model>

<verification>
1. `npx vitest run src/components/LaneHeader src/components/Lane` -- LaneHeader/Lane全テストgreen
2. `npx vitest run` -- Phase 4全テスト + 既存テスト回帰なし
3. ビジュアル検証チェックポイント通過
</verification>

<success_criteria>
- LaneHeader: 課題数括弧表示(D-08)、トグルボタン(D-03)、MemberBreakdown展開(D-03)、9テストgreen
- Lane: computeMemberBreakdown/issueCountをLaneHeaderに渡す、既存+3テストgreen
- 全Phase 4テストgreen、全既存テスト回帰なし
- ビジュアル検証: ステータスカラー、課題数、メンバー内訳、カードクリックが全て動作確認済み
</success_criteria>

<output>
After completion, create `.planning/phases/04-board-enrichment/04-02-SUMMARY.md`
</output>
