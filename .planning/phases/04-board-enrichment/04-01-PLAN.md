---
phase: 04-board-enrichment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/utils/colorContrast.ts
  - src/utils/colorContrast.test.ts
  - src/utils/memberBreakdown.ts
  - src/utils/memberBreakdown.test.ts
  - tests/setup.ts
  - src/components/StatusBadge/StatusBadge.tsx
  - src/components/StatusBadge/StatusBadge.module.css
  - src/components/StatusBadge/StatusBadge.test.tsx
  - src/components/MemberBreakdown/MemberBreakdown.tsx
  - src/components/MemberBreakdown/MemberBreakdown.module.css
  - src/components/MemberBreakdown/MemberBreakdown.test.tsx
  - src/components/IssueCard/IssueCard.tsx
  - src/components/IssueCard/IssueCard.module.css
  - src/components/IssueCard/IssueCard.test.tsx
autonomous: true
requirements:
  - BOARD-04
  - UX-03

must_haves:
  truths:
    - "StatusBadge displays BacklogStatus.color as background with auto-contrast text"
    - "StatusBadge falls back to gray style when color is undefined"
    - "IssueCard click opens correct Backlog URL in default browser via tauri-plugin-opener"
    - "IssueCard shows hover highlight and pointer cursor"
    - "IssueCard has ripple animation on click"
    - "hostUrl is normalized (protocol prefix stripped) before URL construction"
  artifacts:
    - path: "src/utils/colorContrast.ts"
      provides: "WCAG 2.0 luminance-based contrast text color calculation"
      exports: ["getContrastTextColor"]
    - path: "src/utils/memberBreakdown.ts"
      provides: "Member-by-member issue count aggregation"
      exports: ["computeMemberBreakdown", "MemberCount"]
    - path: "src/components/MemberBreakdown/MemberBreakdown.tsx"
      provides: "Sorted member breakdown list component"
      exports: ["MemberBreakdown"]
    - path: "src/components/StatusBadge/StatusBadge.tsx"
      provides: "Status badge with dynamic color from Backlog API"
      exports: ["StatusBadge"]
    - path: "src/components/IssueCard/IssueCard.tsx"
      provides: "Issue card with click-to-open and status color pass-through"
      exports: ["IssueCard"]
  key_links:
    - from: "src/components/StatusBadge/StatusBadge.tsx"
      to: "src/utils/colorContrast.ts"
      via: "getContrastTextColor(color) import"
      pattern: "getContrastTextColor"
    - from: "src/components/IssueCard/IssueCard.tsx"
      to: "src/components/StatusBadge/StatusBadge.tsx"
      via: "color={issue.status.color} prop"
      pattern: "color=.*issue\\.status\\.color"
    - from: "src/components/IssueCard/IssueCard.tsx"
      to: "@tauri-apps/plugin-opener"
      via: "openUrl() on click"
      pattern: "openUrl"
---

<objective>
Phase 4のBoard Enrichmentのうち、ユーティリティ関数、リーフコンポーネント(StatusBadge色対応、MemberBreakdown新規)、IssueCardのクリック動作とリプルアニメーションを実装する。

Purpose: ステータスカラーコーディング(BOARD-04)とカードクリックによるブラウザ遷移(UX-03)を実現し、後続Plan 02のLaneHeader/Lane統合に必要な部品を全て準備する。
Output: colorContrast.ts, memberBreakdown.ts ユーティリティ、StatusBadge色対応、MemberBreakdownコンポーネント、IssueCardクリック+リプル、全テスト
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-board-enrichment/04-CONTEXT.md
@.planning/phases/04-board-enrichment/04-RESEARCH.md
@.planning/phases/04-board-enrichment/04-UI-SPEC.md
@.planning/phases/04-board-enrichment/04-VALIDATION.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From src/types/backlog.ts:
```typescript
export interface BacklogStatus {
  id: number;
  projectId: number | null;
  name: string;
  color: string;
  displayOrder: number;
}

export interface BacklogUser {
  id: number;
  userId: string;
  name: string;
  roleType: number;
  mailAddress: string;
}

export interface BacklogIssue {
  id: number;
  projectId: number;
  issueKey: string;
  keyId: number;
  summary: string;
  description: string | null;
  status: BacklogStatus;
  priority: BacklogPriority | null;
  assignee: BacklogUser | null;
  milestone: BacklogMilestone[];
  category: BacklogCategory[];
  startDate: string | null;
  dueDate: string | null;
  created: string;
  updated: string;
}
```

From src/components/StatusBadge/StatusBadge.tsx (current):
```typescript
interface StatusBadgeProps {
  name: string;
}
export function StatusBadge({ name }: StatusBadgeProps) { ... }
```

From src/components/IssueCard/IssueCard.tsx (current):
```typescript
interface IssueCardProps {
  issue: BacklogIssue;
}
export function IssueCard({ issue }: IssueCardProps) { ... }
// Currently renders: <StatusBadge name={issue.status.name} />
// Currently has: cursor: default in CSS
```

From src/stores/settingsStore.ts:
```typescript
export const useSettingsStore = create<SettingsStoreState>()((set, get) => ({
  settings: { hostUrl: '', apiKey: '', projectId: null, projectKey: '', milestonePrefix: '' },
  // ...
}));
// Access: useSettingsStore((s) => s.settings.hostUrl)
```

From tests/setup.ts (current mocks):
```typescript
vi.mock('@tauri-apps/plugin-http', () => ({ fetch: vi.fn() }));
vi.mock('@tauri-apps/plugin-store', () => ({ load: vi.fn().mockResolvedValue({...}) }));
vi.mock('@tauri-apps/api/core', () => ({ invoke: vi.fn() }));
// NOTE: @tauri-apps/plugin-opener is NOT mocked yet
```

From src/global.css (relevant tokens):
```css
--transition-fast: 150ms ease-out;
--transition-normal: 200ms ease-out;
--color-bg: #F8F9FA;
--color-surface: #FFFFFF;
--color-text-secondary: #6B7280;
--color-border: #D1D5DB;
--radius-badge: 4px;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ユーティリティ関数 + テストインフラ</name>
  <files>
    src/utils/colorContrast.ts
    src/utils/colorContrast.test.ts
    src/utils/memberBreakdown.ts
    src/utils/memberBreakdown.test.ts
    tests/setup.ts
  </files>
  <read_first>
    tests/setup.ts
    src/types/backlog.ts
  </read_first>
  <behavior>
    colorContrast:
    - getContrastTextColor("#ed8077") returns "#ffffff" (red background -> white text)
    - getContrastTextColor("#4caf93") returns "#ffffff" (green-ish -> white text)
    - getContrastTextColor("#f2f2f2") returns "#000000" (light gray -> black text)
    - getContrastTextColor("#ffffff") returns "#000000" (white -> black text)
    - getContrastTextColor("#000000") returns "#ffffff" (black -> white text)
    - getContrastTextColor("#2779bd") returns "#ffffff" (blue -> white text)

    memberBreakdown:
    - computeMemberBreakdown([]) returns []
    - computeMemberBreakdown with 2 issues assigned to same person returns [{ name, count: 2 }]
    - computeMemberBreakdown with 3 issues to 2 people returns sorted by count desc
    - computeMemberBreakdown with 1 unassigned issue returns [{ name: "未割当", count: 1 }] at the end
    - computeMemberBreakdown with mixed assigned/unassigned puts "未割当" last regardless of count
  </behavior>
  <action>
    1. **tests/setup.ts**: 既存の3つのモックの後に `@tauri-apps/plugin-opener` モックを追加:
       ```typescript
       vi.mock('@tauri-apps/plugin-opener', () => ({
         openUrl: vi.fn().mockResolvedValue(undefined),
       }));
       ```

    2. **src/utils/colorContrast.ts**: WCAG 2.0 G17準拠のluminance計算ユーティリティを作成:
       - `hexToRgb(hex: string): [number, number, number]` — `#`除去、2文字ずつparseIntで[r,g,b]に変換
       - `relativeLuminance(r: number, g: number, b: number): number` — sRGB線形化(s <= 0.03928 ? s/12.92 : ((s+0.055)/1.055)^2.4)、重み付き合計 0.2126*R + 0.7152*G + 0.0722*B
       - `export function getContrastTextColor(hexColor: string): string` — luminance > 0.179 なら '#000000'、それ以外は '#ffffff'
       - hexToRgb と relativeLuminance はモジュール内部関数(exportしない)

    3. **src/utils/colorContrast.test.ts**: 上記behaviorの6ケースをテスト。import { getContrastTextColor } from './colorContrast'

    4. **src/utils/memberBreakdown.ts**: メンバー別集計ユーティリティを作成:
       - `export interface MemberCount { name: string; count: number; }`
       - `export function computeMemberBreakdown(issues: BacklogIssue[]): MemberCount[]`
       - Mapで assignee.name をキーにカウント集計。assignee === null は unassignedCount で別カウント
       - Mapエントリを `[...counts.entries()]` で配列化、count降順ソート(per D-04)
       - unassignedCount > 0 なら末尾に `{ name: '未割当', count: unassignedCount }` を追加(per D-04)
       - import type { BacklogIssue } from '../types/backlog'

    5. **src/utils/memberBreakdown.test.ts**: 上記behaviorの5ケースをテスト。テスト内で簡易mockIssueファクトリ関数を作成(BacklogIssue型の必要フィールドのみ)

    TDDサイクル: テストを先に書き(RED)、実装して通す(GREEN)。
  </action>
  <verify>
    <automated>npx vitest run src/utils/colorContrast.test.ts src/utils/memberBreakdown.test.ts -x</automated>
  </verify>
  <acceptance_criteria>
    - src/utils/colorContrast.ts contains `export function getContrastTextColor(`
    - src/utils/colorContrast.ts contains `0.2126` (luminance R coefficient)
    - src/utils/colorContrast.ts contains `0.179` (luminance threshold)
    - src/utils/memberBreakdown.ts contains `export function computeMemberBreakdown(`
    - src/utils/memberBreakdown.ts contains `export interface MemberCount`
    - src/utils/memberBreakdown.ts contains `未割当`
    - tests/setup.ts contains `vi.mock('@tauri-apps/plugin-opener'`
    - tests/setup.ts contains `openUrl: vi.fn()`
    - npx vitest run src/utils/ exits 0
  </acceptance_criteria>
  <done>
    colorContrast: 6テストケースgreen。getContrastTextColorが#000000または#ffffffを正しく返す。
    memberBreakdown: 5テストケースgreen。降順ソート、未割当末尾が正しく動作。
    tests/setup.ts: plugin-openerモック追加済み。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: StatusBadge色対応 + MemberBreakdownコンポーネント</name>
  <files>
    src/components/StatusBadge/StatusBadge.tsx
    src/components/StatusBadge/StatusBadge.module.css
    src/components/StatusBadge/StatusBadge.test.tsx
    src/components/MemberBreakdown/MemberBreakdown.tsx
    src/components/MemberBreakdown/MemberBreakdown.module.css
    src/components/MemberBreakdown/MemberBreakdown.test.tsx
  </files>
  <read_first>
    src/components/StatusBadge/StatusBadge.tsx
    src/components/StatusBadge/StatusBadge.module.css
    src/components/StatusBadge/StatusBadge.test.tsx
    src/utils/colorContrast.ts
    src/utils/memberBreakdown.ts
  </read_first>
  <behavior>
    StatusBadge:
    - StatusBadge with color="#ed8077" renders with inline backgroundColor "#ed8077"
    - StatusBadge with color="#ed8077" renders with inline color from getContrastTextColor (white)
    - StatusBadge without color prop renders without inline style (fallback to CSS)
    - StatusBadge with color renders with borderColor "transparent"

    MemberBreakdown:
    - MemberBreakdown renders each member name and count
    - MemberBreakdown has role="list" and aria-label="メンバー別課題数"
    - MemberBreakdown renders empty when members array is empty (no list rendered)
    - MemberBreakdown truncates long member names with ellipsis (CSS class check)
  </behavior>
  <action>
    1. **StatusBadge.tsx**: 既存コンポーネントを拡張(per D-01, D-02):
       - import { getContrastTextColor } from '../../utils/colorContrast'
       - interface StatusBadgeProps に `color?: string` を追加
       - color が truthy の場合、inline style オブジェクトを生成:
         ```typescript
         const badgeStyle = color
           ? { backgroundColor: color, color: getContrastTextColor(color), borderColor: 'transparent' }
           : undefined;
         ```
       - `<span className={styles.badge} style={badgeStyle} aria-label={name}>`
       - color が undefined/falsy の場合は style=undefined → CSSデフォルト(既存グレー)が適用される

    2. **StatusBadge.module.css**: 変更なし。inline style が CSS を上書きする。既存の .badge スタイルがフォールバックとして機能。

    3. **StatusBadge.test.tsx**: 既存3テストの後に4テスト追加:
       - `it('applies background color from color prop')`: render with color="#ed8077", getByLabelText で要素取得、style.backgroundColor === 'rgb(237, 128, 119)' または '#ed8077' を確認
       - `it('applies contrast text color')`: render with color="#ed8077", style.color が白系か確認
       - `it('renders without inline style when color is undefined')`: render without color prop, style.backgroundColor が空か確認
       - `it('sets transparent border when color is provided')`: render with color="#ed8077", style.borderColor === 'transparent'

    4. **MemberBreakdown.tsx**: 新規コンポーネント作成:
       ```typescript
       import type { MemberCount } from '../../utils/memberBreakdown';
       import styles from './MemberBreakdown.module.css';

       interface MemberBreakdownProps {
         members: MemberCount[];
       }

       export function MemberBreakdown({ members }: MemberBreakdownProps) {
         if (members.length === 0) {
           return null;
         }
         return (
           <ul className={styles.list} role="list" aria-label="メンバー別課題数">
             {members.map((member) => (
               <li key={member.name} className={styles.item}>
                 <span className={styles.memberName}>{member.name}</span>
                 <span className={styles.count}>{member.count}</span>
               </li>
             ))}
           </ul>
         );
       }
       ```

    5. **MemberBreakdown.module.css**: 新規作成:
       ```css
       .list {
         list-style: none;
         padding: var(--space-xs) var(--space-sm);
       }

       .item {
         display: flex;
         justify-content: space-between;
         align-items: center;
         padding: var(--space-xs) 0;
       }

       .memberName {
         font-size: var(--font-size-sm);
         font-weight: var(--font-weight-regular);
         color: var(--color-text-primary);
         overflow: hidden;
         text-overflow: ellipsis;
         white-space: nowrap;
         max-width: 160px;
       }

       .count {
         font-size: var(--font-size-sm);
         font-weight: var(--font-weight-semibold);
         color: var(--color-text-secondary);
         flex-shrink: 0;
         margin-left: var(--space-sm);
       }
       ```

    6. **MemberBreakdown.test.tsx**: 新規作成。4テストケース:
       - members配列でrender → 各name/countがテキストとして表示されるか
       - role="list" と aria-label="メンバー別課題数" の存在確認
       - members=[] で render → list要素がDOMに存在しないか(returns null)
       - memberNameクラスの要素に max-width スタイルがあるか(CSS Modulesなのでクラス存在確認)
  </action>
  <verify>
    <automated>npx vitest run src/components/StatusBadge/StatusBadge.test.tsx src/components/MemberBreakdown/MemberBreakdown.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - src/components/StatusBadge/StatusBadge.tsx contains `color?: string`
    - src/components/StatusBadge/StatusBadge.tsx contains `getContrastTextColor`
    - src/components/StatusBadge/StatusBadge.tsx contains `borderColor: 'transparent'`
    - src/components/StatusBadge/StatusBadge.tsx contains `import { getContrastTextColor } from '../../utils/colorContrast'`
    - src/components/MemberBreakdown/MemberBreakdown.tsx contains `export function MemberBreakdown(`
    - src/components/MemberBreakdown/MemberBreakdown.tsx contains `role="list"`
    - src/components/MemberBreakdown/MemberBreakdown.tsx contains `メンバー別課題数`
    - src/components/MemberBreakdown/MemberBreakdown.module.css contains `.memberName`
    - src/components/MemberBreakdown/MemberBreakdown.module.css contains `max-width: 160px`
    - npx vitest run src/components/StatusBadge src/components/MemberBreakdown exits 0
  </acceptance_criteria>
  <done>
    StatusBadge: color propでAPIカラーが背景に適用される。未指定時は既存グレーにフォールバック。全7テストgreen。
    MemberBreakdown: メンバー名+件数リストが正しくレンダリング。空配列でnull返却。全4テストgreen。
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: IssueCardクリック動作 + リプルアニメーション + 色パススルー</name>
  <files>
    src/components/IssueCard/IssueCard.tsx
    src/components/IssueCard/IssueCard.module.css
    src/components/IssueCard/IssueCard.test.tsx
  </files>
  <read_first>
    src/components/IssueCard/IssueCard.tsx
    src/components/IssueCard/IssueCard.module.css
    src/components/IssueCard/IssueCard.test.tsx
    src/stores/settingsStore.ts
    src/types/backlog.ts
    tests/setup.ts
  </read_first>
  <behavior>
    - IssueCard click calls openUrl with 'https://{hostUrl}/view/{issueKey}' (per D-06)
    - IssueCard click normalizes hostUrl by stripping https:// prefix (Pitfall 2)
    - IssueCard click normalizes hostUrl by stripping http:// prefix
    - IssueCard click silently catches openUrl failure (no throw, no toast)
    - IssueCard has role="link" and aria-label="{issueKey}をBacklogで開く"
    - IssueCard passes issue.status.color to StatusBadge as color prop (per D-01)
    - IssueCard renders with cursor: pointer (CSS class check)
  </behavior>
  <action>
    1. **IssueCard.tsx**: 既存コンポーネントを拡張(per D-05, D-06, D-07):
       - 先頭にimport追加:
         ```typescript
         import { openUrl } from '@tauri-apps/plugin-opener';
         import { useSettingsStore } from '../../stores/settingsStore';
         ```
       - コンポーネント内にhostUrl取得とクリックハンドラ追加:
         ```typescript
         const hostUrl = useSettingsStore((s) => s.settings.hostUrl);

         const handleClick = async () => {
           const host = hostUrl.replace(/^https?:\/\//, '');
           const url = `https://${host}/view/${issue.issueKey}`;
           try {
             await openUrl(url);
           } catch {
             // opener failure is non-critical -- silently ignore
           }
         };
         ```
       - div要素にイベントとアクセシビリティ属性を追加:
         ```tsx
         <div
           className={styles.card}
           onClick={handleClick}
           role="link"
           aria-label={`${issue.issueKey}をBacklogで開く`}
         >
         ```
       - StatusBadgeにcolor propを追加(per D-01):
         ```tsx
         <StatusBadge name={issue.status.name} color={issue.status.color} />
         ```

    2. **IssueCard.module.css**: hover/ripple/cursorスタイル追加(per D-05):
       - `.card` に追加:
         ```css
         cursor: pointer;
         position: relative;
         overflow: hidden;
         transition: background-color var(--transition-fast);
         ```
       - `cursor: default;` を `cursor: pointer;` に変更(既存行を置換)
       - hover状態追加:
         ```css
         .card:hover {
           background-color: var(--color-bg);
         }
         ```
       - リプルアニメーション追加:
         ```css
         .card:active::after {
           content: '';
           position: absolute;
           inset: 0;
           background: radial-gradient(circle at 50% 50%, rgba(0, 0, 0, 0.08) 0%, transparent 60%);
           animation: ripple 300ms ease-out forwards;
         }

         @keyframes ripple {
           from {
             opacity: 1;
             transform: scale(0.5);
           }
           to {
             opacity: 0;
             transform: scale(2);
           }
         }
         ```

    3. **IssueCard.test.tsx**: 既存7テストの後にテスト追加。settingsStoreのモック方法:
       ```typescript
       import { openUrl } from '@tauri-apps/plugin-opener';
       import { useSettingsStore } from '../../stores/settingsStore';
       import userEvent from '@testing-library/user-event';
       ```
       - settingsStoreのモック: `vi.mock('../../stores/settingsStore')` でモジュール全体をモック。各テストのbeforeEachで `vi.mocked(useSettingsStore).mockReturnValue('example.backlog.com')` のようにセレクタ結果を返す。
         ただし、useSettingsStoreは `(selector) => selector(state)` パターンなので、以下のようにモック:
         ```typescript
         vi.mock('../../stores/settingsStore', () => ({
           useSettingsStore: vi.fn((selector: (s: any) => any) =>
             selector({ settings: { hostUrl: 'example.backlog.com' } })
           ),
         }));
         ```
       - テストケース:
         1. `it('calls openUrl with correct Backlog URL on click')`: userEvent.click → expect(openUrl).toHaveBeenCalledWith('https://example.backlog.com/view/TEST-1')
         2. `it('strips https:// from hostUrl')`: hostUrlを 'https://example.backlog.com' にモック → openUrl が 'https://example.backlog.com/view/TEST-1' で呼ばれる(二重プロトコルなし)
         3. `it('strips http:// from hostUrl')`: hostUrlを 'http://example.backlog.com' にモック → 同上
         4. `it('does not throw when openUrl fails')`: openUrlをreject設定 → クリックしてもエラーなし
         5. `it('has role link and aria-label')`: getByRole('link', { name: 'TEST-1をBacklogで開く' }) が存在
         6. `it('passes status color to StatusBadge')`: render後、StatusBadgeにcolor propが渡されているか確認(StatusBadge要素のstyle.backgroundColorで確認)
  </action>
  <verify>
    <automated>npx vitest run src/components/IssueCard/IssueCard.test.tsx -x</automated>
  </verify>
  <acceptance_criteria>
    - src/components/IssueCard/IssueCard.tsx contains `import { openUrl } from '@tauri-apps/plugin-opener'`
    - src/components/IssueCard/IssueCard.tsx contains `useSettingsStore`
    - src/components/IssueCard/IssueCard.tsx contains `replace(/^https?:\/\//, '')`
    - src/components/IssueCard/IssueCard.tsx contains `role="link"`
    - src/components/IssueCard/IssueCard.tsx contains `をBacklogで開く`
    - src/components/IssueCard/IssueCard.tsx contains `color={issue.status.color}`
    - src/components/IssueCard/IssueCard.module.css contains `cursor: pointer`
    - src/components/IssueCard/IssueCard.module.css contains `@keyframes ripple`
    - src/components/IssueCard/IssueCard.module.css contains `.card:hover`
    - npx vitest run src/components/IssueCard/IssueCard.test.tsx exits 0
  </acceptance_criteria>
  <done>
    IssueCard: クリックでopenUrl呼び出し。hostUrl正規化済み。ripple CSS実装。color prop StatusBadgeにパススルー。全13テストgreen。
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| hostUrl -> URL construction | settingsStoreのhostUrlがURL構築に使用される。悪意ある値でURL injection可能 |
| BacklogStatus.color -> inline style | APIから取得したcolor値がinline styleに挿入される |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | IssueCard URL construction | mitigate | hostUrlから `https?://` プレフィックスを除去し、`https://` を固定プレフィックスとして付与。`hostUrl.replace(/^https?:\/\//, '')` で正規化 (Task 3) |
| T-04-02 | Tampering | StatusBadge inline style | accept | BacklogStatus.colorはReact inline style object経由で適用。React がCSSプロパティ値をサニタイズ。dangerouslySetInnerHTML不使用。XSSリスクなし |
</threat_model>

<verification>
1. `npx vitest run src/utils/ src/components/StatusBadge src/components/MemberBreakdown src/components/IssueCard` — Plan 01の全テストgreen
2. `npx vitest run` — 既存テストの回帰なし
3. StatusBadge: color prop あり/なし両方のレンダリング確認
4. IssueCard: openUrl が正しいURLで呼ばれることをテストで確認
5. MemberBreakdown: ソート順と未割当末尾をテストで確認
</verification>

<success_criteria>
- colorContrast.ts: WCAG 2.0準拠のluminance計算、6テストケースgreen
- memberBreakdown.ts: assignee集計、降順ソート、未割当末尾、5テストケースgreen
- StatusBadge: color prop追加、inline style適用、フォールバック動作、7テストgreen
- MemberBreakdown: 新規コンポーネント、リスト表示、4テストgreen
- IssueCard: click->openUrl、hostUrl正規化、ripple CSS、色パススルー、13テストgreen
- tests/setup.ts: plugin-openerモック追加
- 全既存テスト回帰なし
</success_criteria>

<output>
After completion, create `.planning/phases/04-board-enrichment/04-01-SUMMARY.md`
</output>
