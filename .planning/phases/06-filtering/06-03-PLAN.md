---
phase: 06-filtering
plan: 03
type: execute
wave: 3
depends_on: ["06-01", "06-02"]
files_modified:
  - src/App.tsx
  - src/components/Board/Board.tsx
  - src/components/Board/Board.module.css
  - src/components/Board/Board.test.tsx
  - src/components/Lane/Lane.tsx
  - src/components/Lane/Lane.module.css
  - src/components/Lane/Lane.test.tsx
autonomous: false
requirements:
  - FILT-04
  - FILT-05

must_haves:
  truths:
    - "FilterBarがBoardHeaderの直下に表示される"
    - "Board内でuseMemoによりフィルタ済みissuesがLaneに渡される"
    - "boardStore.dataはraw unfilteredのまま変更されない"
    - "フィルタで全カード非表示のレーンに'{N}件がフィルタで非表示'と表示される"
    - "フィルタ適用中もDnDが正常に動作する(ID解決がunfilteredデータを使う)"
    - "Boardのheight calcがFilterBar分を考慮している"
  artifacts:
    - path: "src/App.tsx"
      provides: "FilterBarをBoardHeaderの直下にレンダリング"
      contains: "FilterBar"
    - path: "src/components/Board/Board.tsx"
      provides: "useMemoでフィルタ済みissuesを計算しLaneに渡す"
      contains: "applyFilters"
    - path: "src/components/Lane/Lane.tsx"
      provides: "hiddenCount propを受け取り、フィルタ非表示レーンのメッセージ表示"
      contains: "hiddenCount"
  key_links:
    - from: "src/components/Board/Board.tsx"
      to: "src/stores/filterStore.ts"
      via: "useFilterStore selectors"
      pattern: "useFilterStore"
    - from: "src/components/Board/Board.tsx"
      to: "src/utils/filterUtils.ts"
      via: "applyFilters in useMemo"
      pattern: "applyFilters"
    - from: "src/components/Board/Board.tsx"
      to: "src/stores/boardStore.ts"
      via: "DnD handlers use unfiltered data"
      pattern: "findLaneContaining.*data"
---

<objective>
FilterBarをアプリケーションに統合し、Boardコンポーネントでフィルタ済みデータをLaneに渡す。Laneにフィルタ非表示件数表示を追加する。DnDとフィルタの非干渉を保証する。

Purpose: Plan 01(基盤)とPlan 02(UI)の成果物をBoardに接続し、エンドツーエンドでフィルタリングが動作する完成状態にする。D-09(boardStore.data raw unfiltered)を厳守しつつ、フィルタ済みビューをLaneに渡す。
Output: App.tsx, Board.tsx, Board.module.css, Lane.tsx, Lane.module.css の更新
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-filtering/06-CONTEXT.md
@.planning/phases/06-filtering/06-RESEARCH.md
@.planning/phases/06-filtering/06-UI-SPEC.md
@.planning/phases/06-filtering/06-01-SUMMARY.md
@.planning/phases/06-filtering/06-02-SUMMARY.md

<interfaces>
<!-- Types and contracts from Plan 01/02 + existing codebase -->

From src/types/filter.ts (Plan 01):
```typescript
export interface FilterState {
  statusIds: Set<number>;
  assigneeIds: Set<number | null>;
  categoryIds: Set<number>;
}
```

From src/stores/filterStore.ts (Plan 01):
```typescript
export const useFilterStore = create<FilterStoreState>()((set, get) => ({
  statusIds: Set<number>,
  assigneeIds: Set<number | null>,
  categoryIds: Set<number>,
  toggleStatus: (id: number) => void,
  toggleAssignee: (id: number | null) => void,
  toggleCategory: (id: number) => void,
  removeFilter: (axis: 'status' | 'assignee' | 'category', id: number | null) => void,
  clearAll: () => void,
  hasActiveFilters: () => boolean,
}));
```

From src/utils/filterUtils.ts (Plan 01):
```typescript
export function applyFilters(issues: ReadonlyArray<BacklogIssue>, filters: FilterState): BacklogIssue[];
```

From src/components/FilterBar/FilterBar.tsx (Plan 02):
```typescript
export function FilterBar(): JSX.Element | null;
// No props -- reads from stores directly
```

From src/App.tsx (current):
```typescript
function App() {
  // ...
  return (
    <>
      <BoardHeader onSettingsOpen={() => setShowSettings(true)} />
      <Board />
      {showSettings && <SettingsModal onClose={() => setShowSettings(false)} />}
    </>
  );
}
```

From src/components/Board/Board.tsx (current):
```typescript
export function Board() {
  const data = useBoardStore((s) => s.data);
  // DnD handlers: handleDragStart, handleDragOver, handleDragEnd
  // All use `data` (unfiltered) for ID resolution
  // Renders: Lane for unassigned + Lane for each milestone
}
```

From src/components/Lane/Lane.tsx (current):
```typescript
interface LaneProps {
  laneId: string;
  name: string;
  startDate: string | null;
  releaseDueDate: string | null;
  issues: BacklogIssue[];
  milestonePrefix: string;
  isDropTarget?: boolean;
}
// Renders EmptyLane when issues.length === 0
```

From src/components/EmptyLane/EmptyLane.tsx:
```typescript
export function EmptyLane() {
  return <div className={styles.empty}>課題なし</div>;
}
```

From src/components/Board/Board.module.css:
```css
.board {
  display: flex;
  flex-direction: row;
  overflow-x: auto;
  height: calc(100vh - 56px);
  padding: var(--space-md);
  gap: var(--space-md);
  background: var(--color-bg);
  scroll-behavior: smooth;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Board統合 (FilterBar配置 + useMemoフィルタ + Lane hiddenCount)</name>
  <files>
    src/App.tsx,
    src/components/Board/Board.tsx,
    src/components/Board/Board.module.css,
    src/components/Board/Board.test.tsx,
    src/components/Lane/Lane.tsx,
    src/components/Lane/Lane.module.css,
    src/components/Lane/Lane.test.tsx
  </files>
  <read_first>
    - src/App.tsx (FilterBar配置先を確認)
    - src/components/Board/Board.tsx (既存のDnDハンドラ、Laneレンダリング全体を確認)
    - src/components/Board/Board.module.css (現在のheight calc確認)
    - src/components/Board/Board.test.tsx (既存テストパターンを確認、フィルタ統合テスト追加先)
    - src/components/Lane/Lane.tsx (現在のProps、EmptyLane表示ロジック確認)
    - src/components/Lane/Lane.module.css (現在のmax-height calc確認)
    - src/components/Lane/Lane.test.tsx (既存テストパターン確認)
    - src/components/EmptyLane/EmptyLane.tsx (EmptyLaneの構造確認)
    - src/components/EmptyLane/EmptyLane.module.css (EmptyLaneのスタイルパターン確認)
    - src/stores/filterStore.ts (Plan 01作成のstore API)
    - src/utils/filterUtils.ts (Plan 01作成のapplyFilters)
    - src/components/FilterBar/FilterBar.tsx (Plan 02作成のFilterBar)
    - .planning/phases/06-filtering/06-UI-SPEC.md (Layout Contract、Filtered-Empty Lane State)
  </read_first>
  <action>
**1. src/App.tsx 変更:**

FilterBarをBoardHeaderの直下、Boardの直上に追加:

```typescript
import { FilterBar } from './components/FilterBar/FilterBar';

// return部分を変更:
return (
  <>
    <BoardHeader onSettingsOpen={() => setShowSettings(true)} />
    <FilterBar />
    <Board />
    {showSettings && <SettingsModal onClose={() => setShowSettings(false)} />}
  </>
);
```

FilterBarはsticky配置(top: 56px, z-index: 9)なのでDOMの順序がそのまま視覚的な配置を決定する。

**2. src/components/Board/Board.tsx 変更:**

追加import:
```typescript
import { useMemo } from 'react'; // 既存のuseEffect, useStateに追加
import { useFilterStore } from '../../stores/filterStore';
import { applyFilters } from '../../utils/filterUtils';
import type { FilterState } from '../../types/filter';
```

Board関数内にフィルタ状態の取得とuseMemo計算を追加:
```typescript
// 既存の data, status, error, fetchBoard, moveIssue セレクターの後に追加:
const statusIds = useFilterStore((s) => s.statusIds);
const assigneeIds = useFilterStore((s) => s.assigneeIds);
const categoryIds = useFilterStore((s) => s.categoryIds);

// フィルタ適用 (D-09: dataはraw unfiltered、ビュー層でのみフィルタ)
const filteredView = useMemo(() => {
  if (!data) return null;
  const filters: FilterState = { statusIds, assigneeIds, categoryIds };
  const hasFilters = statusIds.size > 0 || assigneeIds.size > 0 || categoryIds.size > 0;

  const unassignedFiltered = hasFilters
    ? applyFilters(data.unassignedIssues, filters)
    : data.unassignedIssues;

  return {
    milestones: data.milestones.map((mwi) => {
      const filtered = hasFilters ? applyFilters(mwi.issues, filters) : mwi.issues;
      return {
        milestone: mwi.milestone,
        filteredIssues: filtered,
        hiddenCount: hasFilters ? mwi.issues.length - filtered.length : 0,
      };
    }),
    unassigned: {
      filteredIssues: unassignedFiltered,
      hiddenCount: hasFilters ? data.unassignedIssues.length - unassignedFiltered.length : 0,
    },
  };
}, [data, statusIds, assigneeIds, categoryIds]);
```

**重要: DnDハンドラは変更しない。** `handleDragStart`, `handleDragOver`, `handleDragEnd` は全て `data` (unfiltered) を使ってID解決する。これがD-09の保証。`findLaneContaining(data, ...)` と `resolveOverLaneId(data, ...)` はそのまま。

Lane レンダリング部分の変更:
```typescript
// 現在:
<Lane issues={data.unassignedIssues} ... />
{data.milestones.map(({ milestone, issues }) => (
  <Lane issues={issues} ... />
))}

// 変更後: filteredViewを使う
{filteredView && (
  <>
    <Lane
      laneId="unassigned"
      name="未割り当て"
      startDate={null}
      releaseDueDate={null}
      issues={filteredView.unassigned.filteredIssues}
      hiddenCount={filteredView.unassigned.hiddenCount}
      milestonePrefix={milestonePrefix}
      isDropTarget={overLaneId === 'unassigned'}
    />
    {filteredView.milestones.map(({ milestone, filteredIssues, hiddenCount }) => (
      <Lane
        key={milestone.id}
        laneId={`milestone-${milestone.id}`}
        name={milestone.name}
        startDate={milestone.startDate}
        releaseDueDate={milestone.releaseDueDate}
        issues={filteredIssues}
        hiddenCount={hiddenCount}
        milestonePrefix={milestonePrefix}
        isDropTarget={overLaneId === `milestone-${milestone.id}`}
      />
    ))}
  </>
)}
```

ガード条件: `if (status === 'loaded' && data !== null)` はそのまま。filteredViewはdataがnon-nullなら常にnon-null。

**3. src/components/Board/Board.module.css 変更:**

`.board`のheight calcを更新。FilterBarの高さは可変(min-height: 44px)だが、stickyなのでflexboxで自動調整される。

```css
.board {
  display: flex;
  flex-direction: row;
  overflow-x: auto;
  flex: 1;
  min-height: 0;
  padding: var(--space-md);
  gap: var(--space-md);
  background: var(--color-bg);
  scroll-behavior: smooth;
}
```

`height: calc(100vh - 56px)` を `flex: 1; min-height: 0;` に変更。

App.tsxには現在CSSレイアウトがない(React Fragmentをルート)ため、App.tsxのレンダリングをdiv wrapperに変更:

```typescript
// App.tsx return部分:
import styles from './App.module.css';
// ...
return (
  <div className={styles.appLayout}>
    <BoardHeader onSettingsOpen={() => setShowSettings(true)} />
    <FilterBar />
    <Board />
    {showSettings && <SettingsModal onClose={() => setShowSettings(false)} />}
  </div>
);
```

App.module.cssの内容(存在確認し、なければ作成、あれば`.appLayout`を追加):
```css
.appLayout {
  display: flex;
  flex-direction: column;
  height: 100vh;
  overflow: hidden;
}
```

**4. src/components/Lane/Lane.tsx 変更:**

LanePropsにhiddenCountを追加:
```typescript
interface LaneProps {
  laneId: string;
  name: string;
  startDate: string | null;
  releaseDueDate: string | null;
  issues: BacklogIssue[];
  milestonePrefix: string;
  isDropTarget?: boolean;
  hiddenCount?: number;  // 追加: フィルタで非表示のカード数
}
```

cardList内のempty状態ロジック変更:
```typescript
{issues.length === 0 ? (
  hiddenCount && hiddenCount > 0 ? (
    <div className={styles.filteredEmpty}>
      {hiddenCount}件がフィルタで非表示
    </div>
  ) : (
    <EmptyLane />
  )
) : (
  issues.map((issue) => (...))
)}
```

**5. src/components/Lane/Lane.module.css 変更:**

`.filteredEmpty`スタイル追加:
```css
.filteredEmpty {
  padding: var(--space-2xl) var(--space-md);
  text-align: center;
  font-size: var(--font-size-sm);
  color: var(--color-text-secondary);
}
```

`.lane`のmax-heightもflexbox対応に更新:
```css
.lane {
  /* max-height: calc(100vh - 56px - 32px); を削除し以下に変更 */
  max-height: 100%;
  /* 他のプロパティはそのまま */
}
```

**6. テスト更新:**

`src/components/Board/Board.test.tsx`:
- filterStoreのモックを追加(vi.mockまたはbeforeEachでuseFilterStoreをモック)
- フィルタ適用時にLaneがフィルタ済みissuesを受け取ることを検証
- DnDハンドラがunfilteredデータを使うことの保証(既存テストが通り続けることで暗黙的に検証)

`src/components/Lane/Lane.test.tsx`:
- hiddenCount > 0 かつ issues.length === 0 のときに「N件がフィルタで非表示」が表示されることを検証
- hiddenCount === 0 かつ issues.length === 0 のときに EmptyLane(「課題なし」)が表示されることを検証
- hiddenCount未指定 かつ issues.length === 0 のときに EmptyLane(「課題なし」)が表示されることを検証
  </action>
  <verify>
    <automated>npx vitest run src/components/Board/Board.test.tsx src/components/Lane/Lane.test.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/App.tsx に `import { FilterBar }` と `<FilterBar />` を含む
    - src/App.tsx の FilterBar が BoardHeader と Board の間にレンダリングされている
    - src/components/Board/Board.tsx に `import { useFilterStore }` と `import { applyFilters }` を含む
    - Board.tsx に `useMemo` を使った `filteredView` 計算がある
    - Board.tsx のDnDハンドラ(handleDragStart, handleDragOver, handleDragEnd)が `data` (unfiltered) を参照している(filterStoreを参照していない)
    - Board.tsx のLaneレンダリングで `hiddenCount` propを渡している
    - src/components/Lane/Lane.tsx の LaneProps に `hiddenCount?: number` がある
    - Lane.tsx に 「件がフィルタで非表示」テキストを含む
    - src/components/Lane/Lane.module.css に `.filteredEmpty` クラスがあり `--font-size-sm` と `--color-text-secondary` を参照
    - `npx vitest run src/components/Board/Board.test.tsx src/components/Lane/Lane.test.tsx` が exit code 0
    - `npx vitest run` (全テスト) が exit code 0
  </acceptance_criteria>
  <done>FilterBarがApp内でBoardHeader直下に配置され、Board内でuseMemoフィルタ適用、Laneがフィルタ非表示件数を表示、DnDが正常動作。全テストパス。</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: フィルタリング機能の動作確認</name>
  <files>N/A</files>
  <action>
ユーザーがPhase 6フィルタリング機能の動作を手動検証する。以下の手順を実施:

1. `npm run tauri dev` でアプリを起動
2. ボードが表示されたら、FilterBarの3つのドロップダウン(ステータス/担当者/カテゴリ)が表示されていることを確認
3. ステータスドロップダウンをクリック -> チェックボックスリストが表示される -> 1つチェック -> 即座にフィルタが適用され、チップがドロップダウン右側に表示される
4. さらにもう1つチェック -> OR条件で両方のステータスのカードが表示される
5. 担当者ドロップダウンも同様に操作 -> AND条件でステータスと担当者の両方に一致するカードのみ表示
6. フィルタで全カードが非表示になったレーンに「N件がフィルタで非表示」と表示されることを確認
7. チップの x ボタンをクリック -> そのフィルタ条件のみ解除
8. 「すべてクリア」をクリック -> 全フィルタ解除、全カード表示
9. フィルタ適用中にカードをドラッグして別レーンに移動 -> 正常にDnD完了
10. ドロップダウン外をクリックして閉じることを確認
11. Tab/Enter/Escapeキーでドロップダウン操作できることを確認
  </action>
  <verify>
    <automated>npx vitest run</automated>
  </verify>
  <done>ユーザーが上記11項目の動作確認を完了し "approved" と回答</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| boardStore.data -> filteredView | unfilteredデータからフィルタ済みビューへの派生。元データは変更されない |
| user filter selection -> filterStore | ユーザーのUI操作がフィルタ状態を変更。値はissue ID範囲に限定 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | T (Tampering) | Board.tsx filteredView | mitigate | useMemoの読み取り専用派生データ。boardStore.dataは変更しない(D-09)。DnDハンドラは常にdata(unfiltered)を参照 |
| T-06-06 | D (Denial of Service) | Board.tsx useMemo | accept | useMemoの再計算はfilterStore変更時のみ。通常のissue数(数百件)では問題なし |
| T-06-07 | I (Information Disclosure) | Lane hiddenCount | accept | フィルタ非表示件数は数値のみ。issue内容は表示しない。ローカルアプリでありネットワーク漏洩リスクなし |
</threat_model>

<verification>
- `npx vitest run` (全テストスイート) が全テストパス
- FilterBarがBoardHeader直下にsticky配置される
- フィルタ適用時にLaneがフィルタ済みissuesのみ表示する
- boardStore.dataが変更されていない(DnDハンドラがunfilteredデータを使う)
- フィルタで全カード非表示のレーンに件数メッセージが表示される
</verification>

<success_criteria>
- FilterBarがApp内でBoardHeaderの直下に配置(sticky top:56px)
- Boardがフィルタ状態をuseMemoで適用しLaneにフィルタ済みissuesを渡す
- DnDハンドラは常にboardStore.data(unfiltered)を使いID解決(D-07, D-08, D-09)
- Laneがissues.length === 0 && hiddenCount > 0のとき「N件がフィルタで非表示」を表示(FILT-05)
- 全テストスイートがパス
- ユーザーが動作確認でapproved
</success_criteria>

<output>
After completion, create `.planning/phases/06-filtering/06-03-SUMMARY.md`
</output>
