import { describe, it } from 'vitest';

describe('buildSnapshot (dispatcher)', () => {
  it.todo('routes format=json to buildJsonSnapshot and returns string');
  it.todo('routes format=markdown to buildMarkdownSnapshot and returns string');
  it.todo('routes format=csv to buildCsvSnapshot and returns string');
  it.todo('uses shared composeView result for all three formats');
});
